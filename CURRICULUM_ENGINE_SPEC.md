<!-- CURRICULUM_ENGINE_SPEC.md — عظم نظام المنهج والدروس والتقييمات.
     ضعه في جذر المشروع جنب باقي المستندات. يبني فوق TEACHER_MODULE_SPEC.md
     مباشرة، لا يستبدله. -->

# محرك المنهج والمهام الست وتفسير الأرقام

## 0) الإطار العام — اقرأه أولاً

هذا المستند **لا يبني محتوى تعليمي**. يبني ثلاث طبقات فاضية جاهزة لاستقبال:
(أ) محتوى حقيقي من تحضيرات صاحب المشروع، (ب) طلاب حقيقيين (ولو تجريبيين) لتوليد أرقام حقيقية.

**لا تخترع أي محتوى درس. لا تخترع أي سؤال. ابنِ الهيكل (Schema + منطق) فقط.**

---

## 1) آلية "حل تمارين الكتاب" — بسيطة عمداً

**لا علاقة لها بأي محتوى كتاب أو منهج مُحضَّر.** حقل نصي حر يدخل فيه المدرس أرقام صفحات
(نص عادي، مثال: `"4، 5، 6"`)، بدون أي تحقق أو تفسير من النظام لمحتوى الصفحات.

```ts
type BookExerciseTask = {
  id: UUID;
  session_id: UUID;         // مرتبط بالحصة الحالية
  student_group_id: UUID;
  pages_text: string;       // نص حر كامل، كما كتبه المدرس تماماً
  context: "in_session" | "homework"; // نفس الآلية، مكانان مختلفان فقط
  created_at: string;
};
```

**واجهة المدرس:** كارت بعنوان "حل تمارين الكتاب" (خط تحته أسود)، زر "أدخل الصفحات"، حقل نص واحد.
يظهر مرتين مستقلتين: داخل قائمة مهام الحصة، وداخل قائمة الواجب المنزلي — نفس المكوّن، Context مختلف.

---

## 2) المهام الست — خريطة كاملة (الموجود والجديد)

| # | المهمة | النوع | الحالة |
|---|---|---|---|
| 1 | سحب طالب عشوائي للسؤال | أوتوماتيك | ✅ موجود (`TEACHER_MODULE_SPEC.md` §7-هـ) |
| 2 | إرسال الواجب/المهمة لصفحة الطالب | أوتوماتيك | ✅ موجود (§7-ز) |
| 3 | تقييم الواجب المنزلي (لمسة سريعة) | يدوي سريع | ✅ موجود — أول خطوة في كل حصة (§7-أ) |
| 4 | السلوك | يدوي سريع | ✅ موجود (§8، `teacher.assessments.tsx`) |
| 5 | تمارين الكتاب — داخل الحصة | يدوي سريع | 🆕 قسم 1 أعلاه |
| 6 | تمارين الكتاب — كواجب منزلي | يدوي سريع | 🆕 قسم 1 أعلاه (نفس الكيان، `context: "homework"`) |

**تأكيد صريح لصاحب المشروع:** المهام 1، 2، 3، 4 **لا تحتاج أي بناء جديد** — الشغل الفعلي الجديد
في هذا المستند يقتصر على المهمتين 5 و6 فقط، وهما بسيطتان هندسياً (حقل نص).

---

## 3) توقيت تقييم الواجب — ⚠️ تصحيح خطأ توثيقي مني

**اعتراف صريح:** كتبت في هذا القسم سابقاً إن تقييم الواجب "أول خطوة" — **هذا غير دقيق**.
راجعت `TEACHER_MODULE_SPEC.md` §7-ب الأصلي (المصدر الحقيقي المتفق عليه من البداية) ووجدته
ينص صراحة: **1) الشرح التفاعلي 30 دقيقة، 2) تقييم الواجب المنزلي + رفع الغياب 10 دقائق**.
يعني **الشرح هو الخطوة الأولى فعلياً، وتقييم الواجب ثانٍ** — وهذا **يطابق تماماً**
`SESSION_FLOW` الفعلي في `teacher.session.tsx` (lesson → homework → questions → release).

**الإصلاح المطلوب إذاً معكوس عما ادّعيته سابقاً:** لا تُغيَّر `SESSION_FLOW` (هو الصحيح).
بدلاً من ذلك، **صحّح إعلان `SESSION_STEPS` في `mock-data.ts`** ليطابق نفس الترتيب
(lesson → homework → questions → release) — تعديل بسيط في ترتيب مصفوفة إعلان، وليس تغيير
منطق واجهة يعمل بشكل صحيح بالفعل.

**منطق تخطي الخطوة في أول حصة لكل مجموعة** يبقى مطلوباً كما هو — غير موجود حالياً، يُبنى
من الصفر.

---

## 4) اللقطة التاريخية الدائمة لكل درس — ⚠️ فجوة حقيقية، ليست موجودة بالكامل

**تصحيح ادّعاء سابق:** `SessionRecord.lesson_id` موجود ومربوط صح. لكن `AssessmentScore.lesson_id`
**فاضٍ دائماً حالياً** — لا يوجد مسار كود واحد يملأه بقيمة حقيقية (لا من `teacher.assessments.tsx`
ولا من دوال وضع الحصة `scoreHomework`/`recordQuestionAnswer`، اللي بتكتب على `LiveScore`/
`SessionEvent`/`HomeworkTask` فقط). **هذا يجب إصلاحه أولاً قبل أي قسم آخر يعتمد عليه**
(القسمين 5 و6 تحت) — بالضبط كما حدد كلود كود في خطة التنفيذ، هذا أعلى أولوية تقنية.

---

## 5) نظرة الطالب الإجمالية على مستوى المادة — 🆕 جديد

في `student.index.tsx`: قسم جديد "مستواك في [اسم المادة]" لكل مادة الطالب مشترك فيها، يُحسب
كـ Rollup عبر كل `AssessmentScore` المرتبطة بالطالب في هذه المادة تحديداً (عبر `lesson.subject_id`):

```ts
function getSubjectPerformanceSummary(studentId: UUID, subjectId: UUID) {
  const scores = getStudentScoresForSubject(studentId, subjectId); // كل الدروس المسجَّلة
  const currentAvg = average(scores.filter(s => s.lesson_id === latestLessonId));
  const previousAvg = average(scores.filter(s => s.lesson_id === previousLessonId));
  return {
    overallAvg: average(scores),
    trend: currentAvg > previousAvg ? "up" : currentAvg < previousAvg ? "down" : "same",
    lessonsRecordedCount: uniqueLessons(scores).length,
  };
}
```

يتحدّث تلقائياً بمجرد تسجيل درجات درس جديد — لا إدخال يدوي إضافي.

---

## 6) محرك الكروت الوصفية الذكية (Insight Cards) — 🆕 جديد، الأهم

### أ) تصنيف عام حسب المتوسط — **الحدود النهائية من صاحب المشروع (10/25/40/65/85)**

```ts
function getPerformanceLabel(avgPercent: number): string {
  if (avgPercent >= 85) return "متفوق";
  if (avgPercent >= 65) return "جيد جداً";
  if (avgPercent >= 40) return "متوسط، يحتاج تحسين";
  if (avgPercent >= 25) return "ضعيف";
  if (avgPercent >= 10) return "يحتاج متابعة عاجلة";
  return "خطر — يحتاج تدخل فوري";
}
```
ست فئات، وليست خمساً — هذه هي القيم النهائية المعتمدة، لا حاجة لأي تعديل إضافي عليها.

### ب) تشخيص نقطة الضعف تلقائياً

```ts
function diagnoseWeakPoint(studentId: UUID, subjectId: UUID) {
  const components = {
    "الواجب المنزلي": avgOf(AssessmentScore, { category: "homework", source: "manual" }),
    "الواجب الإلكتروني": avgOf(AssessmentScore, { category: "e_homework", source: "auto" }),
    "النشاط": avgOf(AssessmentScore, { category: "activity" }),
    "السلوك": avgOf(AssessmentScore, { category: "behavior" }),
  };
  const sorted = Object.entries(components).sort((a, b) => a[1] - b[1]);
  const [weakestName, weakestValue] = sorted[0];
  const [strongestName, strongestValue] = sorted[sorted.length - 1];

  if (strongestValue - weakestValue < 10) {
    return { hasWeakPoint: false, text: "الأداء متوازن عبر كل الجوانب" };
  }
  return {
    hasWeakPoint: true,
    text: `نقطة ضعفه: ${weakestName} (متوسط ${weakestValue}% مقابل باقي الأنشطة)`,
  };
}
```

### ج) مثال ناتج فعلي (يطابق مثال صاحب المشروع بالحرف)

> "مستوى أحمد في اللغة الإنجليزية حالياً: **جيد، يحتاج تحسين** (65%). نقطة ضعفه: **الواجب
> الإلكتروني** (متوسط 40% مقابل باقي الأنشطة 75%+). ننصح بمتابعته في حل الواجبات الإلكترونية
> تحديداً."

**ملاحظة:** جدول القيم (85/70/50/30) نقطة بداية معقولة مقترحة مني — قابلة للتعديل لاحقاً بسهولة
لأنها كلها في دالة واحدة مركزية، وليست متناثرة في الكود.

---

## 7) اشتراك الطالب الانتقائي بمواد محددة — 🆕 تعديل بيانات

```ts
Student {
  ...existing fields,
  subject_ids: UUID[];   // ⚠️ NEW — بدل الافتراض الضمني الحالي (مادة واحدة عبر group_id فقط)
}
```

شاشة إضافة طالب (عند المالك/الموظف) تعرض checkboxes للمواد الخمسة، يُختار منها ما يناسب هذا
الطالب. صفحة الطالب (`student.index.tsx`) تعرض قسم "مستواك في..." (قسم 5 أعلاه) **فقط**
للمواد الموجودة في `subject_ids` الخاصة به.

---

## 8) عظم المنهج (Curriculum Skeleton) — الهيكل الكامل الفاضي

### ⚠️ قرار نهائي من صاحب المشروع (يُنفَّذ فوراً)

المشروع الحقيقي **ابتدائي/إعدادي فقط**. **احذف المراحل الثانوية القديمة بالكامل** (`gd-1/gd-2/gd-3`
والبيانات المرتبطة بها كمرحلة دراسية) — لا تبقيها كخيار موازٍ. المدرسون الخمسة أنفسهم (بمواد
عربي/إنجليزي/رياضيات/دراسات/علوم من `DESIGN_ATMOSPHERE_SPEC.md`) يبقوا كما هم، فقط `grade_id`
الخاص بمجموعاتهم يُحدَّث ليشير لمراحل ابتدائية بدل الثانوية.

**العدد الآن: 6 مراحل بس** (أول ابتدائي → سادس ابتدائي). **لكن يُبنى الجدول والعلاقات
بشكل قابل للتمدد وليس هاردكود على رقم 6** — لأن إضافة 3 مراحل إعدادية لاحقاً ستكون **نسخاً
مباشراً لنفس تركيبة مواد رابعة/خامسة/سادسة ابتدائي (علوم+دراسات+عربي+إنجليزي+رياضيات) بمحتوى
مختلف فقط**. يعني: `Grade` جدول مرجعي عادي (`id, name, order`)، وربط المادة بالمرحلة بيانات
(`GradeSubject` أو مكافئ)، **وليس شرطاً مكتوباً في الكود** (زي `if (grade === "رابعة")`) —
إضافة مرحلة سابعة مستقبلاً = إدخال بيانات جديد، صفر تعديل كود.

```
Grade (المرحلة)          — 6 الآن: أول/تاني/تالت/رابع/خامس/سادس ابتدائي (قابل للتمدد لـ9 لاحقاً)
  └─ Subject (المادة)      — أول/تاني/تالت: عربي، إنجليزي، رياضيات فقط
       └─ CurriculumUnit    — رابع/خامس/سادس: + علوم + دراسات
            └─ CurriculumLesson (الدرس المخطط)
                 └─ Lesson (الدرس الفعلي عند الرفع) ─┬─ LessonSlide[]  (الشرح)
                                                        ├─ QuizQuestion[] (متنوعة: اختيار/صح-خطأ/ترتيب/توصيل)
                                                        ├─ ElectronicHomework (نفس تنوع الأسئلة)
                                                        ├─ BookExerciseTask (قسم 1 — مرتان: حصة/واجب)
                                                        └─ SuggestedActivity (نشاط واحد حسب موضوع الدرس)
```

**كل هذا فاضٍ من المحتوى الآن.** المدرس يفتح المرحلة → يلاقي المواد → يلاقي الوحدات →
يلاقي الدروس بحالتها (لم يبدأ/قيد التنفيذ/تم) — **بمجرد رفع أي درس، كل الهيكل فوقه يتفعّل
تلقائياً** (نفس آلية "ذاكرة التشغيل" من `TEACHER_MODULE_SPEC.md` §9).

### أنواع الأسئلة المطلوبة (تنوّع، وليس نوعاً واحداً)

```ts
type QuestionType = "mcq" | "true_false" | "ordering" | "matching";
```
تُستخدم في: الأسئلة العشوائية داخل الحصة، وواجب الويب سايت — نفس البنك، سياقان مختلفان.

### النشاط المقترَح لكل درس (مثال أخناتون كمرجع للمنطق، وليس محتوى جاهزاً)

```ts
SuggestedActivity {
  lesson_id: UUID;
  type: "visit" | "draw" | "observe" | "practice" | "other";
  title: string;       // "ننصح بزيارة مدينة أخناتون بالمنيا"
  description: string; // سبب الزيارة وعلاقتها بعمق فهم الدرس
}
```
يُحدَّد **حسب موضوع كل درس تحديداً** وقت رفعه — ليس نمطاً ثابتاً واحداً يتكرر لكل الدروس.

---

## 9) خارج نطاق اليوم — مؤجَّل بوضوح

- **شات جماعي لكل مرحلة (مدرس + طلابه)**: **✅ مؤجَّل بتأكيد صاحب المشروع** — لا يُبنى اليوم
  إطلاقاً، حتى لو ظهر وقت فاضٍ. يُضاف لاحقاً كمهمة منفصلة في `PROJECT_ROADMAP.md` وقت الحاجة
- تسجيل الدخول ودعوة العملاء (فيديو الشرح، إنشاء يوزر/باسورد) — من ضمن مهمة 10 في
  `PROJECT_ROADMAP.md` (هوية دخول موحدة)، ليست اليوم

---

## 10) خطة اليومين الفعلية (بناءً على النطاق المتفق عليه)

| اليوم | المحتوى |
|---|---|
| **اليوم** | بناء العظم كامل (كل ما سبق) + رفع ملف تحضير واحد حقيقي كتجربة + إضافة 4-5 طلاب تجريبيين + تجربة دورة كاملة (رفع → شرح → سؤال → واجب → درجة → كارت وصفي) |
| **بعده** | باقي الأدوار بالحد الأدنى (ربط بيانات حقيقية فقط، بدون تعديل شكل) حسب `PROJECT_ROADMAP.md` |

---

## 11) بيانات الطلاب التجريبيين — جاهزة لخطوة "التجربة الحقيقية" (البند الأخير في §10)

**السياق المحدد:** الصف الرابع الابتدائي، مادة الدراسات الاجتماعية (نفس المادة والمرحلة اللي
درس "أدوات تحديد المواقع" التجريبي مبني عليه). يُربَطوا بمجموعة مدرس الدراسات (أياً كان من
الأربعة المُعاد توزيعهم في `DESIGN_ATMOSPHERE_SPEC.md`) بعد ما `grade_id` بتاعه يتحدَّث للصف
الرابع ابتدائي ضمن هذه الخطة. **القالب قابل للتكرار** على أي مرحلة/مادة تانية لاحقاً بنفس
الطريقة — 5 أسماء، تباين واقعي في الأرقام يغطي كل فئات §6-أ.

```ts
[
  { name: "يوسف أحمد سامي",     attendance_rate: 95, avg_score: 88 }, // متفوق
  { name: "جنى محمد رضا",       attendance_rate: 90, avg_score: 72 }, // جيد جداً
  { name: "مازن حسن علي",       attendance_rate: 78, avg_score: 55 }, // متوسط، يحتاج تحسين
  { name: "ملك إبراهيم فتحي",   attendance_rate: 60, avg_score: 32 }, // ضعيف
  { name: "آدم كريم صلاح",      attendance_rate: 40, avg_score: 15 }, // يحتاج متابعة عاجلة
]
```

كل طالب `subject_ids` بتاعه يحتوي مادة الدراسات فقط الآن (اختبار مباشر لقسم 7 — الاشتراك
الانتقائي بمواد محددة).

---

## 13) إعادة هيكلة وضع الحصة — المنهج جوّه الحصة + توحيد مصدر التقييم

### أ) الفكرة الجوهرية
`teacher/session/$groupId` يتحول من "شاشة حصة واحدة معزولة" إلى **المركز الكامل لكل ما يخص
هذه المجموعة تحديداً** — المنهج، الطلاب، وكل أنواع التقييم، في مكان واحد.

### ب) المنهج داخل وضع الحصة (يمين الشاشة)
- عند الدخول لمجموعة معينة، يظهر عمود جانبي بمنهج **هذه المجموعة بالتحديد** (وحدة → دروس →
  امتحانات، بنفس تسلسل §8/§12)، مبني من `CurriculumUnit`/`CurriculumLesson` المرتبطة
  بـ`grade_id`/`subject_id` مجموعة هذا الـ`groupId`
- المدرس يضغط على درس معين من القائمة → **هذا الدرس تحديداً** هو اللي يفتح في منطقة العرض
  الرئيسية (الشرح + الأسئلة + الواجب + النشاط) — **ليس** `getLatestReadyLesson` التلقائي
  الحالي، بل اختيار صريح من المدرس
- رفع PDF جديد يرتبط بـ**فتحة درس محددة مُختارة من القائمة**، وليس "الدرس التالي" المُخمَّن تلقائياً
- بعد إتمام الحصة (نهاية الخطوات الأربع + إنهاء الحصة)، الدرس يتحول لحالة "تم" في القائمة —
  **لكن يبقى قابلاً للفتح لاحقاً في وضع "مراجعة" (Read-only)**: يعرض ملخص ما حدث (الدرجات،
  الحضور، حالة الأنشطة) بدل إعادة فتح التايمر الحي من جديد

### ج) توحيد مصدر التقييم — **قرار نهائي مُعدَّل من صاحب المشروع**

**لا يوجد تبويب منفصل غير موقوت خالص.** كل أنواع التقييم (واجب، حضور، سلوك، أنشطة) تصبح
**جزءاً من التسلسل الموقوت لكل درس على حدة**، تتكرر بنفس الشكل مع كل حصة. **التسلسل النهائي
المؤكَّد من صاحب المشروع (9 خطوات، وليس 4 كما كان سابقاً):**

| # | الخطوة | الوقت | ملاحظة |
|---|---|---|---|
| 1 | تقييم واجب الحصة اللي فاتت + الحضور | 10 دقائق | أول خطوة، تقييم آخر مرة اتحطت |
| 2 | الشرح التفاعلي | 25 دقيقة | |
| 3 | الأسئلة العشوائية والتوزيع | 8 دقائق | |
| 4 | حل مهام الكتاب (`BookExerciseTask`، §1) | 3 دقائق | المدرس يُدخل أرقام الصفحات فقط |
| 5 | مراجعة النشاط المقترح | 3 دقائق | عرض `SuggestedActivity` المولَّد تلقائياً مع الدرس، المدرس يراجعه فقط تمهيداً للإطلاق — لا إدخال هنا |
| 6 | إطلاق واجب البيت (تمارين الكتاب) | 2 دقيقة | نفس آلية `BookExerciseTask`، `context: "homework"` |
| 7 | إطلاق واجب الويب سايت (`ElectronicHomework`) | 2 دقيقة | |
| 8 | حالة النشاط | بلا وقت مستقل | لو اترفعت درجاته داخل الحصة (خطوة 5) يُقفل، وإلا ينضم تلقائياً لقائمة الواجبات المُطلَقة في خطوة 6/7 |
| 9 | **تقييم السلوك** | 5 دقائق | آخر خطوة إلزامياً — يحتاج ملاحظة المدرس للطالب طوال الحصة قبل الحكم عليه |

**الإجمالي ≈ 58 دقيقة.** كل خطوة من التسعة (عدا 8، تلقائية) لها مؤقتها المستقل الخاص، وكل
تقييم مربوط إلزامياً بـ`session_id` و`lesson_id` الحصة الجارية وقت التسجيل، بلا استثناء.
- **`teacher.assessments.tsx` يتحول بالكامل لتقرير مُجمَّع للقراءة فقط** — فلتر حسب المجموعة،
  ويعرض: عدد الطلاب، تقرير شامل مُجمَّع من كل الحصص السابقة (متوسط الواجب، الحضور، السلوك،
  الأنشطة) لكل طالب ولكل المجموعة ككل. **صفر نماذج إدخال فيه إطلاقاً**
- **تعديل بيانات درس سابق بأثر رجعي**: يحدث عبر فتح ذلك الدرس تحديداً من "وضع المراجعة"
  (§13-ب)، وليس عبر شبكة منفصلة في صفحة التقييمات

### هـ) نظرة الطالب الإجمالية — تمديد لـ §5 (كيان جديد على مستوى الطالب ككل، وليس مادة واحدة)

بالإضافة لـ"مستواك في [مادة معينة]" (§5 الموجود)، تُضاف طبقة أعلى: **"مستواك العام"** —
متوسط مُجمَّع عبر **كل المواد** اللي الطالب مشترك فيها (`subject_ids` من §7)، يظهر أعلى صفحة
الطالب فوق تفاصيل كل مادة على حدة:

```ts
function getOverallStudentPerformance(studentId: UUID) {
  const subjectSummaries = student.subject_ids.map(sid =>
    getSubjectPerformanceSummary(studentId, sid)  // من §5، موجودة بالفعل
  );
  return {
    overallAvg: average(subjectSummaries.map(s => s.overallAvg)),
    bySubject: subjectSummaries,  // تفصيل كل مادة تحته مباشرة
  };
}
```
دالة تجميع فوق دوال موجودة بالفعل — ليست إعادة بناء لمنطق الحساب، مجرد طبقة عرض أعلى.

---

## 12) ⏸️ الامتحانات الأسبوعية والشهرية — 🆕 مُسجَّلة الآن، **لا تُنفَّذ اليوم**

> **تنبيه صريح لكلود كود: هذا القسم إضافة للطابور فقط. لا تبدأ فيه إطلاقاً قبل إتمام كل بنود
> §10 (الخطة الحالية التسعة) بالكامل والتأكد من التجربة الحقيقية الناجحة أولاً.**

### الفكرة
كيان جديد `Exam` مُشتق من الدروس (وليس محتوى مستقل)، يظهر **داخل قائمة الوحدة نفسها** بالتتابع
مع الدروس (لا قسم منفصل):

```
الوحدة الأولى
  ├─ الدرس 1
  ├─ الدرس 2
  ├─ 📝 امتحان أسبوعي (مُشتق من الدرس 1+2)
  ├─ الدرس 3
  ├─ الدرس 4
  ├─ 📝 امتحان أسبوعي (مُشتق من الدرس 3+4)
  ├─ ... (حتى 4 امتحانات أسبوعية = 8 دروس)
  └─ 📘 امتحان شهري شامل (مُشتق من الأربعة امتحانات الأسبوعية معاً)
```

### البنية

```ts
Exam {
  id: UUID; unit_id: UUID
  kind: "weekly" | "monthly"
  title: string                    // "امتحان أسبوعي — الدرس 1+2"
  source_lesson_ids: UUID[]        // أسبوعي: درسان، شهري: كل دروس الأربعة أسابيع
  questions: QuizQuestion[]        // مُنتقاة من بنوك أسئلة الدروس المصدر (تنويع الأنواع من §8)
  position_after_lesson_id: UUID   // مكانه في تسلسل عرض الوحدة
}

ExamAssignment {
  id: UUID; exam_id: UUID; group_id: UUID
  mode: "in_class" | "homework"    // نفس منطق تمارين الكتاب — حصة أو بيت
  assigned_at: string
}

ExamScore {
  id: UUID; exam_id: UUID; student_id: UUID
  score: number; max_score: number; recorded_at: string
}
```

### متطلبات إضافية
- **تصميم بصري عالي الجودة** يطابق هوية `DESIGN_ATMOSPHERE_SPEC.md` (ألوان المادة، خط، تناسق) —
  ليس نموذجاً نصياً عادياً
- **تصدير PDF** قابل للتحميل (نفس مهارة/أداة PDF المستخدمة في أي جزء آخر من المشروع لاحقاً)
- **الدرجة تتغذى في نفس محرك التقييم** (§5 و§6) — لا نظام تقييم موازٍ، نفس `AssessmentScore`
  بفئة جديدة (`category: "exam"`)

### لماذا مؤجَّل تحديداً
يعتمد بالكامل على استقرار §8 (عظم المنهج) و§4 (ربط `AssessmentScore.lesson_id`) أولاً —
بناؤه قبلهم يعني بناء فوق أساس غير مؤكد، بالضبط نفس المبدأ اللي اتبع طول المشروع
("المنتِج قبل المستهلِك").