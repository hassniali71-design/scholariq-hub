# تقرير التطوير الشامل — صفحة المدرس (`/teacher/*`)

تقرير ختامي لسلسلة التعديلات المتتالية على وحدة المدرس، بعد الطلب الأصلي بتنفيذ
"تطوير وإصلاح شامل" (تكامل الفيتشرز + مزامنة البيانات + UI/UX + منطق العمل) على
أساس فحص الكود الفعلي سطر بسطر، بدون بيانات وهمية أو إصلاحات شكلية. التنفيذ تم
على 6 دفعات commit منفصلة (كل دفعة = مهمة هندسية واحدة محددة، مُتحقَّق منها بـ
`tsc --noEmit` قبل كل push)، بالإضافة لجولة تحقق نهائية شملت تشغيل `bun run lint`
فعلياً — وهي كشفت خللاً حقيقياً (مش شكلياً) تم إصلاحه فوراً (تفصيل في القسم 4).

---

## 1) ملخص الدفعات المُنفَّذة

| # | Commit | الموضوع |
|---|---|---|
| 1 | `f062eb6` | صورة المدرس الحقيقية مدموجة في كارت واحد مع الاسم (زي كارت الطالب بالضبط)، إزالة الهيدر المكرر لاسم المدرس. |
| 2 | `ea33fa5` | نسب كل وسام/تنبيه لاسم وهوية المدرس اللي بعته (`teacher_id`/`teacher_name` على `whatsapp_logs`، migration 0036)، وظهوره فعلياً عند الطالب (لوحة الشرف) وولي الأمر (الرسائل) بنفس الاسم والتوقيت. |
| 3 | `4ec5e12` | زر "إضافة ملاحظة" في كارت تصنيف الطالب بقى بيوجّه لنفس فورم الأوسمة/التنبيهات الحقيقي (`AwardsAndAlertsPanel`) بدل حقل نص حر منفصل كان بيضيع بلا أثر. حذف `TeacherNoteInput.tsx` الميت. |
| 4 | `d4fff20` | صفحة "الخطة والمنهج": الفلتر الأساسي بقى الصف الدراسي (مش المجموعة) — مدرس بيدرّس نفس الصف في مجموعتين بيشوف كل الخطط/الإحصائيات مجمَّعة على مستوى الصف أولاً. توحيد مكان تسجيل السلوك: اتشالت نسخة مكررة قابلة للتعديل من صفحة "التقييمات والغياب" (المفروض تكون قراءة فقط)، والتسجيل الحقيقي الوحيد بقى من داخل وضع الحصة (`AssessmentsTable`، تبويب "السلوك"). حذف `BehaviorScoreColumn.tsx` الميت. |
| 5 | `d74fc7e` | سجل الحضور في "التقييمات والغياب": من جداول منفصلة لكل مجموعة (مصفوفة طالب×تاريخ) إلى **جدول واحد موحّد**، صف لكل طالب، يلخّص حاضر/متأخر/غائب الحقيقية من `attendanceRecords` + آخر حصة وحالتها — مفلتر تلقائياً بنفس فلتر المرحلة/المجموعة أعلى الصفحة. |
| 6 | `12fc101` | تنبيه جديد للمالك (في "قرارات تحتاج اتخاذ") لطلاب ببيانات تسجيل ناقصة: طالب بدون مجموعة فعلية مرتبطة (`group_id` فاضي رغم وجود اسم مجموعة)، أو طالب بدون أي مادة مسجَّلة (`subject_ids` فاضية) — الحالتان كانتا بتختفيان بصمت من كل الشاشات، دلوقتي بتظهر صراحة. |
| 7 | `e97104f` | **إصلاح حرج** اكتُشف بتشغيل `bun run lint` فعلياً — تفصيل كامل تحت (القسم 4). |

كل دفعة اتعملها commit منفصل ودُفعت لـ `main` و`claude/fix-group-students-secondary-enrollment` فور التحقق.

---

## 2) نقاط تحقّقنا إنها **حقيقية بالفعل** ولم تحتج أي تعديل

قبل تعديل أي حاجة، الفحص المباشر للكود أثبت إن الأنظمة دي شغالة صح فعلياً ومش
ديكور، فاتسيبت زي ما هي (تعديلها كان هيبقى إعادة اختراع بلا داعي):

- **الشات طالب↔مدرس** (`StudentMessagesCard.tsx` + `dm-messages.ts` + `QuickMessageModal`):
  فعلي بالكامل ومحفوظ دائم — نمط `notifications` بـ`kind: "dm:<studentId>:<teacherId>"`،
  اتنين الطرفين بيكتبوا ويقروا من نفس السجل الحقيقي.
- **تسجيل السلوك الحقيقي** داخل وضع الحصة (`AssessmentsTable`، مقياس 0-10، تبويب
  "السلوك") — موجود وصحيح من الأساس؛ المشكلة كانت وجود نسخة تانية مكررة قابلة
  للتعديل في مكان تاني (اتشالت، القسم 1 دفعة 4).
- **CRUD الخطة والمنهج**: `createLessonPlan`/`updateLessonPlan`/`deleteLessonPlan`/
  `markLessonPlanState` (`data-store.ts`) كلهم بيحدّثوا الحالة المحلية **و**
  يزامنوا فعلياً مع Supabase (`upsertLessonPlanRow`/`deleteLessonPlanRow`) —
  مفيش أي عملية منهم بتضيع بعد الخروج/الدخول.
- **`GroupCurriculumTimeline`/`GroupResourcesPanel`** (روابط منهج وإطلاقات المدرس):
  `addGroupResource`/`deleteGroupResource`/`addTeacherLaunch`/`deleteTeacherLaunch`
  كلها حقيقية ومتزامنة، وحذف إطلاق مهمة بيشيل معاه محاولات الطلاب المرتبطة به
  (`homeworkAttempts`) — cascade صحيح، بدون بقايا يتيمة.
- **أرقام الطالب الحقيقية** (`attendance_rate`/`avg_score`/`points`) اتأكد إنها
  بتتحدّث فوراً من `recomputeStudentStats` عند كل حضور/تقييم — مش صفر ثابت،
  ومنعكسة صح في كل الجداول الجديدة اللي بنيناها هذه الدفعة.

---

## 3) نطاق لم يُطلَب تعديله في هذه الدفعة

بنود من المواصفة الأصلية خارج نطاق دفعات "الإصلاح الشامل" الست دي لأنها فيتشرز
جديدة كبيرة (مش إصلاح/ربط بيانات موجودة)، ومؤجلة صراحة في المواصفة الأصلية نفسها
(رفع درس PDF بتوليد شرح/أسئلة تلقائي بالـAI) — لم تُلمَس هنا.

---

## 4) الإصلاح الحرج المُكتشَف بتشغيل `bun run lint`

`tsc --noEmit` بيتحقق من الأنواع بس — مش قواعد React نفسها. لما شغّلنا
`bun run lint` فعلياً (بعد كل الدفعات فوق) للتأكد الكامل، ظهر خطأ حقيقي
(`react-hooks/rules-of-hooks`) في **ملفين اتعدَّلوا هذه الجلسة**:
`teacher.assessments.tsx` و`teacher.curriculum.tsx`.

**المشكلة**: `useMemo`/`useState` كانت مكتوبة **بعد** شرط خروج مبكر شرطي
(`if (!isHydrated) return null; if (!teacher) return <Navigate .../>`). ده يعني
عدد الـ Hooks المُستدعاة فعلياً بيختلف بين render وتاني — صفر Hooks قبل ما
الجلسة تخلص تحميل، وست/سبعة Hooks بعدها. React بيعتمد على **نفس الترتيب بالظبط**
في كل مرة عشان يربط كل Hook بحالته الصحيحة؛ كسر الترتيب ده سلوك غير مُعرَّف
رسمياً — ممكن يظهر كخطأ صريح ("Rendered more hooks than during the previous
render")، أو أخطر: يفضل شغال ظاهرياً لكن يربط قيمة `useState` غلط بمكان تاني
بصمت (data corruption صامت في الواجهة، مش crash واضح تقدر تلاحظه بسهولة).

**الإصلاح**: نقل كل الـ Hooks (`useMemo` لحساب الصفوف، `useState` لكل الفلاتر)
لأعلى الدالة، **قبل** أي `return` شرطي، مع تأمين القيم المشتقة من `teacher`
(`myGroups`, `myPlans`) بقيمة افتراضية فاضية طول ما الجلسة لسه بتتحمّل. هذا النوع
من الأخطاء **موجود من قبل تعديلات هذه الجلسة في نفس النمط** (نفس الـ early-return
كان موجود قبل ما نضيف الفلاتر الجديدة) — يعني كان قنبلة موقوتة صامتة من قبل،
واكتشافها جه بالتزامن مع تعديلاتنا لأننا زوّدنا عدد الـ Hooks بعد الـ return،
فزاد احتمال ظهور التعارض الفعلي في المتصفح الحقيقي.

بعد الإصلاح: صفر أخطاء `rules-of-hooks` في كل المشروع. باقي مخرجات `lint`
(حوالي 470 مشكلة) كلها تنسيق `prettier` بحت (مسافات/أسطر) منتشرة في ملفات
لم نلمسها هذه الجلسة (`parent.messages.tsx`, `platform.teacher-notes.tsx`,
`staff.cashier.tsx`, إلخ) — تُركت كما هي عمداً لتفادي commit ضخم غير مرتبط
بمهمة صفحة المدرس؛ الملفين اللي لمسناهم فعلاً (`teacher.assessments.tsx`,
`teacher.curriculum.tsx`) اتصلّحوا تلقائياً بـ`eslint --fix` (تنسيق بحت، بدون
أي تغيير منطقي — تم تأكيد ده بمراجعة الـ diff سطر بسطر).

---

## 5) التحقق المُنفَّذ

- `tsc --noEmit` نظيف بعد كل دفعة (بدون أي خطأ جديد، باستثناء خطأين معروفين
  مسبقاً غير متعلقين بأي تعديل: حزمة Lovable الخاصة و`@types/node` غير
  متاحين في بيئة الجلسة السحابية فقط).
- `bun run lint` تم تشغيله فعلياً (مش بس `tsc`) — كشف واتصلح الخلل الحرج
  في القسم 4.
- **لم يتم اختبار حي على بيانات Supabase حقيقية من هنا** — نفس القيد
  الشبكي الموثّق طول الجلسة (لا يوجد اتصال بقاعدة بيانات المستخدم من
  البيئة السحابية). التحقق الوظيفي الحقيقي محتاج المستخدم يجربه بنفسه على
  الرابط الحي بعد النشر:
  1. تسجيل دخول كمدرس، فتح "التقييمات والغياب" — التأكد إن جدول الحضور
     الجديد ظاهر كجدول واحد (مش جداول منفصلة)، وإن فلتر الصف بيغيّر الجدول
     ده كمان مش بس الجدول العام تحته.
  2. فتح "الخطة والمنهج" — التأكد إن اختيار صف دراسي (مش مجموعة) بيجمّع
     كروت المؤشرات من كل مجموعات نفس الصف مع بعض.
  3. من "التقييمات والغياب": الضغط على "إرسال تنبيه" لطالب من كارت
     "يحتاجون تحسين" — التأكد إنه بيوديك لفورم الأوسمة/التنبيهات مفتوح
     مباشرة على نفس الطالب.
  4. تسجيل دخول كمالك — التأكد من عدم ظهور أي خطأ Hooks في الـConsole عند
     فتح صفحتي "التقييمات والغياب"/"الخطة والمنهج" (F12 → Console، بدون
     أي سطر أحمر "Rendered more hooks").
  5. لو فيه طالب حقيقي بدون مجموعة أو بدون مادة مسجَّلة: التأكد من ظهوره في
     "قرارات تحتاج اتخاذ" عند المالك.

---

## 6) التالي

بعد تأكيد المستخدم إن دفعات صفحة المدرس دي شغالة صح على البيئة الحقيقية، الخطوة
التالية المتفق عليها مسبقاً: جولة سريعة على صفحات الطالب/الكاشير/ولي الأمر، ثم
يُعتبر المشروع جاهز للتسليم.
