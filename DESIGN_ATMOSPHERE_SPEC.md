<!-- DESIGN_ATMOSPHERE_SPEC.md — مواصفات الطبقة البصرية (التصميم + الأجواء).
     ضعه في جذر المشروع جنب CLAUDE.md وTEACHER_MODULE_SPEC.md.
     هذا تعديل بصري/CSS بحت — لا يمس منطق البيانات أو الأنواع أو data-store.ts إطلاقاً. -->

# مواصفات التصميم والأجواء (Design & Atmosphere Layer)

## 0) نطاق هذا المستند — مهم جداً يا Claude Code

هذا **تعديل بصري خالص (Presentation Layer)**. لا يوجد أي تغيير في:
- `data-store.ts` أو أي دالة mutation
- `types/index.ts` (باستثناء إن كان `Subject.theme_key` غير موجود بالفعل — تحقق أولاً)
- أي منطق وظيفي في `owner.*`, `staff.*`, `student.*`, `parent.*`, `visitor.*`

**التطبيق الأولي يقتصر على:** لوحة المدرس الرئيسية (`teacher.index.tsx`) كترويسة "غرفة المادة"، عارض الشرائح داخل وضع الحصة (`teacher.session.tsx` — الألوان والأيقونات فقط، وليس الترتيب أو المنطق)، وقسم نظرة الطلاب في لوحة المالك. باقي الأدوار **لاحقاً**، ليست ضمن هذا التنفيذ.

**الهوية العامة (Sidebar، اللوجو، اسم "سنتر النخبة التعليمي"، الأزرق الكحلي الحالي) تبقى كما هي تماماً — بلا أي تغيير.** الإضافات هنا داخل مساحة المحتوى فقط.

## ⚠️ 0.1) قيد صارم — لا استثناء فيه

**هذا التنفيذ بأكمله CSS/بيانات بذرة/مكوّنات عرض بصري فقط. أي دالة، أي mutation، أي منطق حساب، أي state management موجود حالياً في `data-store.ts` يبقى بلا أي تعديل حرفياً.** لو وجدت أثناء التنفيذ إن تغييراً بصرياً يتطلب تعديل منطق (مثلاً لحساب لون تلقائي)، توقف واسأل قبل ما تكمل — لا تفترض إنه "تعديل بسيط مسموح". القاعدة: **لو مش لون، أيقونة، خط، أو ترتيب بصري بحت — ماتلمسهوش.**

## 0.2) توسعة بيانات البذرة (Seed Data) — مطلوبة قبل التنفيذ البصري

المواد الخمسة المطلوب تمثيلها فعلياً في `mock-data.ts` (كقيمة `subject` بس، **لا تغيّر `grade`/المرحلة الدراسية لأي مدرس أو مجموعة إطلاقاً**): عربي، إنجليزي، رياضيات، دراسات، علوم.

- المدرسون الأربعة الموجودون حالياً (مصطفى الجندي، هبة عبد الرحمن، كريم شوقي، نورهان سعيد): **أعد توزيع حقل `subject` بس عليهم عشوائياً** بين الخمسة مواد دي، بنفس الأسماء، بنفس الـIDs، بنفس ربطهم بمجموعاتهم وطلابهم وكل أرقامهم الحالية (نقاط، حضور، إيرادات) — **بلا أي تغيير في أي حقل تاني غير `subject` والحقول المشتقة منه (زي `theme_key`)**.
- المادة الخامسة الناقصة (بما إن عندنا 4 مدرسين بس و5 مواد): **أضف مدرساً خامساً جديداً** بنفس شكل البيانات وهيكلها بالضبط (نفس الحقول، مجموعة وطلاب مرتبطين بنفس النمط العلائقي من Phase 1 في `TEACHER_MODULE_SPEC.md`)، باسم عربي واقعي، عشان الخمس ثيمات كلها تكون قابلة للمعاينة الفعلية.
- هذا التعديل **محتواه بس** — لا يضيف حقولاً جديدة للأنواع، ولا يغيّر شكل `Teacher`/`Group`/`Student` الحالي.

---

## 1) الاتجاه المرجعي (Reference)

الهيكل العام مستوحى من نمط لوحات تحكم تعليمية نظيفة (نمط "Edu Center — Education Management System"): صف كروت KPI في الأعلى بمساحة بيضاء وحواف دائرية، شبكة محتوى واضحة بدون ازدحام، شارتات بسيطة. **الفرق الجوهري:** فوق نفس الهيكل ده، بنضيف طبقة "أجواء المادة" (لون + أيقونة + اقتباس) في الترويسة بس — الباقي (الكروت، الجداول، الشارتات) يفضل نظيف زي ما هو، **من غير أي زحمة بصرية إضافية**. الهدف "حي مش صاخب".

---

## 2) الهوية الأساسية (Design Tokens)

### الألوان

```css
/* الهوية العامة — بدون تغيير، مرجع فقط */
--brand-navy: #1B3A5C;

/* ألوان المواد — طبقة جديدة، تُستخدم فقط داخل محتوى صفحة المدرس */
--subject-arabic:  #8B2E3F;  /* عربي — عنّابي */
--subject-english: #12777A;  /* إنجليزي — تركواز */
--subject-math:    #5B4B8A;  /* رياضيات — بنفسجي */
--subject-science: #2E7D4F;  /* علوم — أخضر */
--subject-social:  #B8792F;  /* دراسات — كهرماني ترابي */
```

كل لون له نسخة خلفية فاتحة جداً (Tint ~8-10% Opacity) تُستخدم كخلفية لكارت "غرفة المادة" — تُحسب برمجياً (`color-mix` أو نسخة hex فاتحة مخزَّنة يدوياً لكل لون)، وليست حسابية عشوائية وقت التشغيل لتفادي مشاكل التباين.

### الخط

```
IBM Plex Sans Arabic
```
يُستورد من Google Fonts أو يُحمَّل محلياً (`@fontsource/ibm-plex-sans-arabic` عبر npm أفضل من CDN خارجي لضمان الاستقرار دون اتصال). يستبدل الخط الحالي في `styles.css` بالكامل (وليس فقط داخل صفحة المدرس) — هذا القرار الوحيد من هذا المستند الذي يمتد لكل المشروع، لأن تبديل خط واحد ومتسق أفضل من خط مزدوج.

### الحركة

أيقونة الخلفية في "كارت غرفة المادة" تتحرك حركة خفيفة مستمرة (float بسيط، ليست جذابة للانتباه، مجرد إحساس حياة):

```css
@keyframes gentle-float {
  0%   { transform: translateY(0px) rotate(0deg); }
  50%  { transform: translateY(-10px) rotate(4deg); }
  100% { transform: translateY(0px) rotate(0deg); }
}
.subject-room-icon {
  animation: gentle-float 7s ease-in-out infinite;
}
```
احترم `prefers-reduced-motion: reduce` — عطّل الحركة تلقائياً لمن يفضّلها (إمكانية وصول أساسية، ليست اختيارية).

---

## 3) مكوّن "غرفة المادة" (Subject Room Header)

يستبدل الترويسة الحالية في `teacher.index.tsx` (اسم المدرس + المادة كنص طويل). المواصفات:

- خلفية بلون المادة الفاتح (Tint)، حواف دائرية 12px
- أيقونة كبيرة شفافة (Opacity ~0.15-0.18) في الخلفية، بلون المادة الأساسي، بحركة `gentle-float`
- اسم المدرس + المادة (badge صغير بالأيقونة واللون) + عدد المجموعات/الطلاب — **نفس المعلومات الموجودة حالياً، مُعاد تنظيمها بصرياً فقط**
- **كارت الاقتباس اليومي** أسفل الترويسة مباشرة: نص قصير + أيقونة علامة اقتباس، بخلفية بيضاء/محايدة (مش بلون المادة، عشان النص يفضل واضح ومقروء)

### ربط اللون/الأيقونة بالمادة تلقائياً

يعتمد على `Subject.theme_key` (موجود من Phase 2 في `TEACHER_MODULE_SPEC.md`، تحقق من وجوده الفعلي أولاً). لكل `theme_key` قيمة ثابتة في جدول تعيين (Mapping) بالكود:

```ts
const SUBJECT_ROOM_THEME: Record<string, { color: string; icon: string }> = {
  "عربي":       { color: "var(--subject-arabic)",  icon: "book" },
  "إنجليزي":    { color: "var(--subject-english)", icon: "language" },
  "رياضيات":    { color: "var(--subject-math)",     icon: "math-function" },
  "علوم":       { color: "var(--subject-science)", icon: "flask" },
  "دراسات":     { color: "var(--subject-social)",   icon: "world" },
};
```
لا حاجة لأي إدخال يدوي من المدرس — يُشتق تلقائياً عند تسجيل الدخول حسب مادته المسجَّلة.

---

## 3.5) عارض الشرائح والأيقونات داخل وضع الحصة — هوية بصرية بس

`teacher.session.tsx` وعارض الشرائح (`InteractiveSlideViewer` أو المكون المكافئ من `TEACHER_MODULE_SPEC.md` §7-د): يتغيّر **فقط**:
- لون الحدود/العناوين/الأزرار ليطابق لون مادة المدرس الحالي
- الأيقونة المصاحبة (لو موجودة) لتطابق أيقونة المادة من `SUBJECT_ROOM_THEME`
- الخط (IBM Plex Sans Arabic) بما إنه بيتغيّر عالمياً بالفعل من قسم 2

**لا يتغيّر:** ترتيب المراحل الأربعة، منطق التايمر، آلية التمديد، طريقة السحب العشوائي للأسئلة، أو أي سلوك وظيفي — دي كلها ثابتة زي ما اتقفلت في `TEACHER_MODULE_SPEC.md`. هذا تلوين وتزيين بصري للمكوّنات الموجودة، وليس إعادة بناء لها.

---

## 4) الاقتباس اليومي (Daily Quote)

**النطاق الواقعي لهذا التنفيذ:** بنك اقتباسات ثابت صغير في الكود (5-8 اقتباسات لكل مادة، مكتوبة يدوياً الآن)، يُختار منها اقتباس واحد يومياً بشكل حتمي (Deterministic — بناءً على تاريخ اليوم، مش عشوائي بيتغيّر كل ريفريش). **الربط الديناميكي الحقيقي بمحتوى الدرس الفعلي المُدرَّس (عبر `Lesson.extracted_text`) خطوة لاحقة تحتاج استدعاء AI حقيقي — مؤجَّلة حتى تفعيل المفتاح الحقيقي (نفس قيد الـStub في `TEACHER_MODULE_SPEC.md`)، ويُكتفى الآن باقتباس عام مرتبط بالمادة ككل وليس بالدرس تحديداً.**

```ts
const DAILY_QUOTES: Record<string, string[]> = {
  "علوم": [
    "التفاعل الكيميائي إعادة ترتيب، مش اختفاء",
    "كل اكتشاف علمي بدأ بسؤال بسيط: ليه؟",
    // ... 4-6 إضافية لكل مادة
  ],
  // باقي المواد بنفس النمط
};

function getTodayQuote(subject: string): string {
  const list = DAILY_QUOTES[subject] ?? [];
  const dayIndex = new Date().getDate() % list.length;
  return list[dayIndex] ?? "";
}
```

---

## 5) نظرة الطلاب البصرية (لوحة المالك)

استبدال/إضافة لقسم عرض الطلاب في `owner.students.tsx`: كل طالب في كارت صغير بحد جانبي (Border-right) ملوّن + أيقونة تدل على وضعه:

| الحالة | الحد الجانبي | الأيقونة | الشرط |
|---|---|---|---|
| متفوق | `--border-accent` (اللون الأساسي الحالي) | نجمة (`star-filled`) | ضمن أعلى 20% نقاطاً في مجموعته |
| عادي | رمادي محايد | شخص (`user`) | باقي الحالات |
| يحتاج متابعة | رمادي داكن | تنبيه (`alert-circle`) | يطابق شرط `classifyStudent` = "يحتاج متابعة" الموجود بالفعل من `TEACHER_MODULE_SPEC.md` §6-د |

**يعيد استخدام دالة `classifyStudent` الموجودة بالفعل** — لا منطق تصنيف جديد، مجرد عرض بصري مختلف لنفس النتيجة.

---

## 6) قواعد عامة إلزامية

- لا تغيّر أي بيانات أو دوال منطقية — هذا CSS/Component تعديل بحت
- لا تلمس Sidebar أو اللوجو أو التنقل
- احترم `prefers-reduced-motion`
- التطبيق أولاً على لوحة المدرس الرئيسية + نظرة طلاب المالك فقط — لا تمتد لباقي الصفحات في هذا التنفيذ
- تأكد من التباين (Contrast) الكافي بين النص ولون خلفية كل مادة — نص أبيض أو نص غامق حسب حساب تلقائي بسيط (لا تفترض نصاً أبيض ثابتاً لكل الألوان الخمسة)
- بعد التنفيذ: `bun run dev` نظيف، فحص كل صفحات المدرس والمالك بصرياً (Screenshot أو وصف) للتأكد من عدم كسر أي تخطيط موجود

---

## 7) خارج النطاق الآن (لاحقاً)

- تطبيق نفس الأجواء على أدوار الموظف/الطالب/ولي الأمر/الزائر
- اقتباسات مرتبطة فعلياً بمحتوى الدرس (تحتاج AI حقيقي مفعّل)
- Theme كامل قابل للتخصيص من المالك نفسه (ألوان مخصصة لكل سنتر عميل)