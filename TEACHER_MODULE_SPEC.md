<!-- TEACHER_MODULE_SPEC.md — Master Implementation Specification for Teacher ERP Module.
     ضعه في جذر المشروع جنب CLAUDE.md وTEACHER_MODULE_MEMO.md.
     هذا المستند يُقال لكلود كود صراحة: "اقرأ TEACHER_MODULE_SPEC.md ونفّذ Phase 1". -->

# Master Implementation Specification — Teacher ERP Module
### وثيقة المواصفات التنفيذية الكاملة لإعادة هيكلة نظام المدرس

---

## 0) ملاحظة أمانة تقنية — اقرأها أولاً يا Claude Code

هذا التحليل مبني على فحص كامل لكود `scholariq-hub` (سطر بسطر) **قبل** أي تعديلات محلية جرت
معك بعد ذلك (بما فيها Phase 0 — إنشاء `data-store.ts`). **قبل تنفيذ أي بند هنا، نفّذ SCAN
كامل على الحالة الفعلية للمشروع عندك الآن أولاً** (خصوصاً `src/lib/data-store.ts` إن وُجد،
و`src/types/index.ts`)، وصالح أي فرق بين ما هو موصوف هنا وما هو موجود فعلياً — لا تكسر أي
شيء أنجزته سابقاً. إن وجدت تعارضاً، أوقف ونبّه صاحب المشروع قبل الاستمرار.

### 0.1) ملحق تبسيط النطاق (تعديل لاحق من صاحب المشروع — أولوية على أي نص متعارض في باقي المستند)

بعد نقاش إضافي، تقرر تبسيط جزء "رفع الدرس والذكاء الاصطناعي" (قسم 7-د إلى 7-ز) على النحو التالي:

- **لا يوجد مساعد حي/شات بوت أثناء الحصة (Live Teaching Co-pilot).** هذه الفكرة مؤجلة بالكامل وليست جزءاً من Phase 1-5 الحالية. لا تُنفَّذ أي واجهة محادثة حية داخل وضع الحصة.
- **عارض الشرائح (القسم 7-د) ليس مطلوباً أن يكون "تفاعلياً" بمعنى معقد.** المطلوب: سلايدر بسيط قابل للتصفح يدوياً (زر "→ التالي" و"السابق ←") بتصميم ملوّن وواضح، يعرض شرح الدرس بدل فتح ملف الـPDF الخام مباشرة. لا حاجة لأي تفاعل أذكى من التنقل بين الشرائح.
- **الأسئلة المولّدة تقتصر على نوعين فقط**: اختيار من متعدد (MCQ) وصح/خطأ (True/False). لا حاجة لأنواع أسئلة أخرى (مقالي، إلخ) في هذه المرحلة.
- **الواجب الإلكتروني (`ElectronicHomework`) يتبع نفس القيد**: أسئلة MCQ أو True/False فقط، مولَّدة من نفس محتوى الشرائح.
- هذا التبسيط **يقلل التعقيد الهندسي**، وليس إضافة — طبّقه كأولوية على أي وصف أكثر تعقيداً ورد سابقاً في القسم 7 من هذا المستند.

**منهج التنفيذ الإلزامي لكل Phase:**
```
SCAN → UNDERSTAND → PLAN → DATABASE CHECK → IMPLEMENT → TEST → VERIFY → REPORT
```
بعد كل Phase قدّم تقريراً يوضح: ماذا تم، ماذا لم يتم ولماذا، هل `bun run build` نجح، هل
`bun run dev` يعمل بدون أخطاء، هل أي وظيفة قديمة (owner/staff/student/parent/visitor)
تأثرت.

---

## 1) Executive Summary

نعيد هيكلة وحدة المدرس بالكامل من "شكل أساسي بأربع مراحل تايمر ثابتة" إلى **نظام تشغيلي
حقيقي مربوط بقاعدة بيانات علائقية**، مقسّم لأربعة أقسام واضحة في القائمة الجانبية:

| # | القسم | الوظيفة الجوهرية |
|---|---|---|
| 1 | لوحة المدرس | نظرة عامة + KPIs حقيقية + طلاب يحتاجون متابعة |
| 2 | وضع الحصة | تدريس حي فقط: شرح تفاعلي + رفع PDF بالـAI + أسئلة عشوائية + إطلاق مهام |
| 3 | التقييمات والغياب | إدارة كاملة: حضور، تقييم واجب، أنشطة، سلوك، تصنيف طلاب |
| 4 | الخطة والمنهج | "ذاكرة تشغيل" — كل ما يحدث في وضع الحصة ينعكس هنا تلقائياً |

**التغيير الجوهري في الفلسفة:** وضع الحصة يتحول من "شاشة فيها كل شيء بما فيه التقييم اليدوي
الكامل" إلى **شاشة عرض وتدريس سريعة** فقط، بينما الإدارة التفصيلية (تعديل درجة قديمة،
مراجعة سلوك، تقارير) تنتقل لقسم مستقل. هذا يفصل "الأداء اللحظي أثناء الحصة" عن "الإدارة
بعد الحصة" — وهو فصل مطلوب صراحة في المتطلبات وصحيح هندسياً.

---

## 2) تحليل النظام الحالي (Current System Analysis)

### أ) Stack التقني (بلا افتراضات)
TanStack Start (React 19 + TanStack Router file-based + SSR) + Tailwind v4 + shadcn/ui +
Vite 8 + bun. **لا يوجد أي backend حقيقي حالياً** — لا Supabase، لا API، لا env vars. كل
البيانات مصفوفات ثابتة في `src/lib/mock-data.ts`، مع نمط `localStorage + subscribe/emit`
مُثبت وشغال في `src/lib/auth.ts` (وهو النمط الذي بُني عليه `data-store.ts` في Phase 0).

### ب) الجداول/الكيانات الموجودة حالياً (`src/types/index.ts`)
`Student, Teacher, Group, AttendanceRecord, PaymentRecord, BookletItem, SessionStep,
QuizQuestion, LiveScore, LessonSlide, QuizResult, HomeworkTask, WhatsAppLog, TeacherNote,
LeaderboardEntry` + جداول تحليلات (`RevenuePoint, AttendancePoint, PerformancePoint`).

### ج) نظام المصادقة (`src/lib/auth.ts`)
حسابات محلية (`localStorage`) بدون ربط حقيقي بهوية "مدرس" محدد من جدول `Teacher` — تسجيل
الدخول بدور `teacher` لا يحدد **أي مدرس بالتحديد**. هذه فجوة موجودة من الأساس ولازم تُحل
كجزء من Phase 1 (تفصيل في القسم 4).

### د) نظام التنقل الحالي (`src/config/roles.ts`)
```
teacher.nav = [ "لوحة المدرس" → /teacher, "وضع الحصة" → /teacher/session ]
```
قسمين فقط. المطلوب: أربعة، بإضافة `/teacher/assessments` و`/teacher/curriculum`.

### هـ) الفجوات الهيكلية المكتشفة فعلياً في الكود (ليست افتراضات)

| الملف | المشكلة | الأثر |
|---|---|---|
| `teacher.index.tsx` | `me = teachers[0]` ثابت | الصفحة دايماً بتفتح على نفس المدرس |
| `teacher.index.tsx` | "جدول مجموعاتي" يعرض **كل** مجموعات السنتر | مدرس يرى مجموعات مدرسين آخرين |
| `teacher.index.tsx` | "طلاب يحتاجون متابعة" يفلتر **كل** طلاب السنتر | نفس المشكلة |
| `teacher.session.tsx` | `group = groups[0]` ثابت | زر "ابدأ" في أي مجموعة يفتح نفس المجموعة الأولى دائماً |
| `teacher.session.tsx` | `students.slice(0, 6)` | لا علاقة حقيقية بطلاب المجموعة المختارة |
| `mock-data.ts` | `lessonSlides` و`sessionQuestions` **ثابتة عالمياً** | نفس محتوى "الفيزياء" يظهر لأي مادة/مدرس/مجموعة — **لا يوجد كيان "درس" في النظام أصلاً** |
| `Group.teacher_name` | نص وليس معرّف (`teacher_id`) | لا عزل حقيقي بين مدرس وآخر |
| `Student.group_name` | نص وليس معرّف (`group_id`) | لا عزل حقيقي بين مجموعة وأخرى |

هذه الفجوات هي سبب أن Phase 1 يجب أن يبدأ بالبنية العلائقية قبل أي شيء آخر — بالضبط كما
اقترح صاحب المشروع.

---

## 3) القرارات الهندسية المتخذة (لا تحتاج رجوعاً لصاحب المشروع)

| القرار | التفاصيل |
|---|---|
| **Subject وGrade يصبحان كيانات مرجعية حقيقية** | بدل نص حر متكرر في كل جدول، جدولان صغيران `Subject{id,name,theme_key}` و`Grade{id,name,order}`، مع الإبقاء على القيم النصية الحالية بلا تغيير ظاهر |
| **الذكاء الاصطناعي = Server Function بـ System Prompt ثابت مخزَّن في الكود، وليس "تدريب Agent" متكرر** | تفصيل كامل في القسم 10 |
| **العزل بين المدرسين = دوال قراءة مُقيّدة بـ `teacher_id` داخل `data-store.ts`، وليس إخفاء بصري في الواجهة** | يمنع مدرساً من رؤية بيانات مدرس آخر حتى لو عدّل الرابط يدوياً |
| **خوارزمية التوزيع العادل للأسئلة العشوائية** | وزن مركّب: (عدد مرات السحب سابقاً) + (منذ متى آخر سحب) + (هل حضر) + (هل سُحب في هذه الحصة). تفصيل في القسم 7-هـ |
| **تخزين ملف PDF الأصلي غير ممكن بشكل دائم بالبنية الحالية** (لا يوجد backend فعلي، والتخزين محلي `localStorage` محدود ~5MB) | الحل العملي: معالجة الملف عند الرفع فوراً، الاحتفاظ بالنص المُستخرج + المحتوى المولّد فقط (نصوص خفيفة)، وليس بايتات الملف نفسه. هذا قيد حقيقي وليس تجاهلاً — الحل الدائم هو Supabase Storage لاحقاً (مذكور في القسم 14) |

---

## 4) Database Schema — الكيانات الكاملة

### أ) كيانات موجودة تُعدَّل (وليس تُستبدل)

```ts
Teacher {
  ...existing fields,
  user_id: UUID          // ⚠️ NEW — يربط حساب auth.ts بمدرس محدد فعلياً (يحل فجوة "من هو المستخدم الحالي")
  subject_id: UUID        // ⚠️ NEW — بدل subject: string
}

Group {
  ...existing fields,
  teacher_id: UUID         // ⚠️ NEW — الأساس، احتفظ بـ teacher_name نصياً للعرض فقط
  subject_id: UUID         // ⚠️ NEW
  grade_id: UUID           // ⚠️ NEW — بدل grade: string
}

Student {
  ...existing fields,
  group_id: UUID            // ⚠️ NEW — الأساس، احتفظ بـ group_name نصياً للعرض فقط
}

QuizQuestion {
  ...existing fields,
  lesson_id: UUID           // ⚠️ NEW — بدل كونها عالمية ثابتة
  source: "ai_generated" | "manual"
}

LessonSlide {
  ...existing fields,
  lesson_id: UUID           // ⚠️ NEW
}
```

### ب) كيانات جديدة كلياً

```ts
Subject {
  id: UUID; center_id: UUID; name: string; theme_key: string
  // theme_key يحدد Theme اللون (فيزياء/كيمياء/رياضيات/لغة...) — قسم 7-و
}

Grade {
  id: UUID; center_id: UUID; name: string; order: number
}

Lesson {
  id: UUID; center_id: UUID; group_id: UUID; subject_id: UUID
  title: string
  source_file_name: string | null      // اسم الملف الأصلي للعرض فقط، لا البايتات
  extracted_text: string | null         // النص المستخرج من PDF — يُخزَّن، لا يُعاد استخراجه
  content_hash: string | null           // SHA-256 لمحتوى الملف — يمنع إعادة معالجة نفس الملف
  ai_status: "idle" | "processing" | "ready" | "failed"
  ai_error: string | null
  taught_status: "not_started" | "in_progress" | "done"
  taught_at: string | null
  actual_duration_seconds: number | null
  created_by_teacher_id: UUID
}

CurriculumUnit {
  id: UUID; center_id: UUID; subject_id: UUID; grade_id: UUID
  name: string; order: number; planned_duration_days: number
}

CurriculumLesson {
  // "الدرس المخطط" — يختلف عن Lesson (الدرس الفعلي المُدرَّس)
  id: UUID; unit_id: UUID; order: number; title: string
  status: "not_started" | "in_progress" | "done"
  linked_lesson_id: UUID | null   // يربط بالـ Lesson الفعلي بمجرد ما يُدرَّس
}

SessionRecord {
  id: UUID; center_id: UUID; group_id: UUID; lesson_id: UUID | null
  teacher_id: UUID; date: string
  attendees_count: number; absentees_count: number
  questions_asked_count: number; participants_count: number
  homework_launch_status: "not_sent" | "sent"
  duration_seconds: number; explanation_duration_seconds: number
  extension_seconds: number
  general_notes: string | null
}

SessionEvent {
  // سجل لحظي لكل حدث — أساس "لوحة الدرجات اللحظية" + قابلية الاسترجاع لاحقاً
  id: UUID; session_id: UUID; student_id: UUID; at: string
  kind: "homework_score" | "question_answer" | "activity_score" | "attendance" | "note"
  payload: Record<string, unknown>   // تفاصيل الحدث (الدرجة، صح/خطأ، إلخ)
}

TimerExtension {
  id: UUID; session_id: UUID; step_key: SessionStepKey
  added_seconds: number; reason: string | null; at: string
}

AssessmentScore {
  id: UUID; center_id: UUID; student_id: UUID; session_id: UUID | null
  lesson_id: UUID | null
  category: "homework" | "activity" | "behavior" | "question" | "e_homework"
  source: "auto" | "manual"         // ⚠️ الفصل المطلوب صراحة في المتطلبات
  value: number; max_value: number
  recorded_by_teacher_id: UUID; recorded_at: string
}

RandomPickLog {
  // أساس خوارزمية التوزيع العادل
  id: UUID; group_id: UUID; student_id: UUID; session_id: UUID; picked_at: string
}

ElectronicHomework {
  id: UUID; lesson_id: UUID; group_id: UUID
  questions: QuizQuestion[]     // 10-15 سؤال مولّدة بالـAI أو يدوية
  due_at: string
  // ⚠️ ملاحظة نطاق: واجهة "حل الطالب لهذا الواجب" تخص بوابة الطالب، خارج نطاق تعديل
  // المدرس اليوم. هذا الكيان يغطي إنشاء المدرس للواجب فقط — قرار مطلوب في القسم 15
}
```

### ج) خريطة العلاقات (نصياً)

```
Tenant (center) ─┬─ Teacher ──┬── Group ──┬── Student
                  │            │            └── AttendanceRecord (per session)
                  │            └── Lesson ──┬── LessonSlide
                  │                          ├── QuizQuestion
                  │                          └── ElectronicHomework
                  ├─ Subject ── CurriculumUnit ── CurriculumLesson ⇄ Lesson (linked)
                  └─ SessionRecord ──┬── SessionEvent
                                      ├── TimerExtension
                                      └── AssessmentScore
```

### د) Indexes/RLS (بالمعنى العملي لبنيتنا الحالية)
لا يوجد Postgres فعلياً بعد، لكن **مبدأ RLS يُطبَّق منطقياً الآن** عبر دوال `data-store.ts`:
كل دالة قراءة تخص المدرس (`getGroupsForTeacher(teacherId)`, `getStudentsForGroup(groupId)`,
`getLessonsForTeacher(teacherId)`...) **تفلتر داخلياً ولا تُرجع بيانات مدرس آخر أبداً**، حتى
لو استُدعيت بمعرّف خاطئ (ترجع مصفوفة فارغة، لا خطأ يكشف وجود بيانات). هذا يُحاكي RLS فعلياً
ويكون Drop-in سهل عند الانتقال لـ Supabase (نفس الدوال، الاستعلام الداخلي فقط يتغير).

---

## 5) إعادة هيكلة التنقل (`config/roles.ts`)

```ts
teacher: {
  nav: [
    { label: "لوحة المدرس", to: "/teacher", icon: LayoutDashboard },
    { label: "وضع الحصة", to: "/teacher/session", icon: MonitorPlay },
    { label: "التقييمات والغياب", to: "/teacher/assessments", icon: ClipboardCheck },
    { label: "الخطة والمنهج", to: "/teacher/curriculum", icon: BookMarked },
  ],
}
```
ملفات جديدة: `src/routes/teacher.assessments.tsx`, `src/routes/teacher.curriculum.tsx`.

---

## 6) لوحة المدرس (`teacher.index.tsx`) — المواصفات

### أ) الترويسة
```
مصطفى الجندي
دراسات اجتماعية
```
اسم + مادة فقط، بدون الجملة الطويلة الحالية. المادة تُشتق من `Subject` المرتبط بالمدرس.

### ب) KPI Cards — تكبير الحجم + القائمة الكاملة

| # | الكارت | مصدر الحساب |
|---|---|---|
| 1 | عدد الطلاب | `count(Student) where group.teacher_id = me` |
| 2 | الالتزام بالتايمر | متوسط (`session.duration ÷ step.planned_duration`) من `SessionRecord` آخر 30 يوم |
| 3 | حصص هذا الأسبوع | `count(SessionRecord) where date in this_week` |
| 4 | المجموعات | `count(Group) where teacher_id = me` |
| 5 | الطلاب الملتزمون | `count(Student) where attendance_rate >= 90` |
| 6 | الطلاب الغائبون (آخر حصة) | من آخر `SessionRecord` لكل مجموعة |
| 7 | الطلاب المتفوقون | حسب تصنيف 3-مستويات (قسم 6-د) = "ممتاز" |
| 8 | يحتاجون متابعة | نفس التصنيف = "يحتاج متابعة" |

+ مقترَحان إضافيان (اختياري، مفيدان فعلياً وليسا حشواً):
- **متوسط تقدم المنهج** (من `CurriculumLesson` المكتملة ÷ الإجمالي) — يربط الداشبورد بالخطة مباشرة
- **دروس تحتاج مراجعة AI** (عدد `Lesson.ai_status = "failed"`) — تنبيه عملي مفيد

### ج) إزالة "مراحل الحصة" بالكامل من هذه الصفحة
يُحذف القسم كاملاً من `teacher.index.tsx`. المفهوم ينتقل لقسم الخطة والمنهج كـ"منطق تشغيل"،
وليس عرضاً مكرراً.

### د) مخطط "مستوى الطلاب في المادة" (استبدال الشارت الحالي)
شارت واحد لمادة المدرس فقط (وليس فيزياء/كيمياء/رياضيات معاً كما هو الآن) — لكل طالب: الاسم،
المتوسط، والتصنيف. **قواعد التصنيف الثلاثية (افتراضية، قابلة للتعديل — راجع قسم القرارات
المطلوبة رقم 3):**

```
ممتاز:        avg_score >= 85  AND  attendance_rate >= 90
يحتاج متابعة:  avg_score < 60   OR   attendance_rate < 75
متوسط:        كل الحالات الأخرى
```

### هـ) "طلاب يحتاجون متابعة" — تصميم مختصر
لكل طالب: الاسم، الحضور، المتوسط، **سبب التصنيف مُولَّد نصياً** (مثال: "انخفاض متوسط الدرجات
+ ضعف الحضور")، وحقل **"أضف ملاحظتك"** يُخزَّن في `TeacherNote` مرتبطاً بـ `student_id`
و`teacher_id` و`created_at`.

توليد سبب التصنيف (منطق حتمي بسيط، بدون AI هنا):
```
if attendance_rate < 75 → أضف "ضعف الحضور"
if avg_score < 60 → أضف "انخفاض متوسط الدرجات"
if avg question_score آخر 3 حصص < 50% → أضف "ضعف نتائج الأسئلة التفاعلية"
اجمع الأسباب بـ " + "
```

---

## 7) وضع الحصة (`teacher.session.tsx`) — إعادة بناء كاملة

### أ) مبدأ الفصل الجوهري
وضع الحصة = **تدريس وتفاعل لحظي فقط**. أي تعديل يدوي لاحق (مراجعة درجة، تصحيح خطأ، تقييم
سلوك) يذهب لقسم "التقييمات والغياب" المستقل. **الاستثناء الوحيد:** رصد سريع (حضور + تقييم
واجب بلمسة واحدة 0/2/4/6/8/10) يبقى داخل وضع الحصة لأنه جزء من إيقاع الحصة الحي نفسه — هذا
هو التوازن الصحيح بين طلب "لا تقييمات يدوية كاملة هنا" وطلب "تقييم الواجب + الغياب ضمن
تقسيم الحصة". القسم المستقل يقدّم: التاريخ، التعديل بأثر رجعي، السلوك، والتقارير المجمّعة.

### ب) تقسيم الحصة الجديد

| المرحلة | المدة الأساسية | ملاحظة |
|---|---|---|
| الشرح التفاعلي | 30 دقيقة | قابلة للتمديد (انظر ج) |
| تقييم الواجب + رفع الغياب | 10 دقائق | رصد سريع فقط |
| الأنشطة التفاعلية (الأسئلة العشوائية) | 10 دقائق | |
| إطلاق المهام والواجبات | 10 دقائق | |

### ج) نظام التمديد والـScore
- عند انتهاء الوقت الأساسي: زر **"تمديد"** يفتح خيارات `+5 / +10 / مدة مخصصة`
- كل تمديد يُسجَّل في `TimerExtension` (المدة، السبب اختياري، التوقيت)
- **قاعدة الـScore (افتراضية — قرار مطلوب تأكيده، قسم القرارات رقم 4):**
  ```
  أنهى بالضبط في الوقت المخطط        → 100% التزام
  أنهى قبل الوقت (لم يتجاوزه)         → 100% التزام (لا عقاب) + تُسجَّل المدة الفعلية
  استخدم تمديداً حتى X% من الوقت الأساسي  → 100% التزام (تمديد معقول)
  تجاوز التمديد المعقول               → نسبة التزام تتناقص تدريجياً (مش صفر فجأة)
  ```

### د) رفع PDF وتوليد العرض التفاعلي بالـAI

**تدفق العملية (Pipeline محدد وثابت، وليس Prompt يُكتب كل مرة):**

```
1. المدرس يرفع PDF عبر Box واضح ("اسحب ملف PDF هنا / اختر ملف الدرس")
2. حساب content_hash فوراً (SHA-256)
3. فحص: هل نفس الـhash موجود من قبل لنفس الدرس؟
   → إن وجد: استخدم النتيجة المخزَّنة فوراً (0 استدعاء AI إضافي، توفير تكلفة)
   → إن لم يوجد: أكمل للخطوة التالية
4. استخراج النص من PDF (مكتبة استخراج نص، ليس AI — لا داعي لدفع تكلفة AI لمجرد قراءة الملف)
5. استدعاء AI Server Function واحد بـSystem Prompt ثابت مخزَّن في الكود (وليس محادثة تُكتب
   يدوياً كل مرة) — التفاصيل الكاملة في القسم 10
6. تخزين extracted_text + الشرائح المولَّدة + الأسئلة المولَّدة في Lesson
7. عرض تفاعلي فوري (واجهة شبيهة PowerPoint، أزرار "→ التالي / السابق ←")
8. Lesson.ai_status = "ready"، أو "failed" مع ai_error واضح للمدرس مع زر "إعادة المحاولة"
```

**واجهة العرض:** نفس فكرة `LessonStep` الحالية (نقاط عناوين + bullets) لكن بتصميم أوضح
"عرض تقديمي" — عنوان كبير + نقاط بارزة + مؤشر شرائح، بدل الشكل الحالي البسيط.

### هـ) الأسئلة العشوائية — خوارزمية التوزيع العادل

بدل الاختيار العشوائي البسيط الحالي (`Math.random()` على كل الطلاب بالتساوي):

```ts
function pickFairly(students: Student[], log: RandomPickLog[], sessionId: UUID) {
  const weighted = students
    .filter(s => attendedThisSession(s))
    .filter(s => !alreadyPickedThisSession(s, sessionId, log))
    .map(s => ({
      student: s,
      weight:
        1 / (timesPickedTotal(s, log) + 1) +          // كلما قلّ عدد مرات سحبه، وزنه أعلى
        recencyBonus(s, log),                           // كلما طالت مدة عدم سحبه، وزن إضافي
    }));
  return weightedRandomPick(weighted);
}
```
كل سؤال يُسجَّل: الدرس، الحصة، الطالب، السؤال، الإجابة، الوقت، النتيجة، الـScore — عبر
`SessionEvent(kind: "question_answer")` + `AssessmentScore(category:"question", source:"auto")`.
تايمر السؤال قابل للاختيار (10/15/30 ثانية) حسب صعوبة السؤال — يحدده المدرس عند السحب.

### و) نظام Theme حسب المادة (بسيط، قابل للتوسع)
جدول ثابت صغير في الكود (وليس بنية معقدة):
```ts
const SUBJECT_THEMES: Record<string, { primary: string; accent: string }> = {
  "الدراسات": { primary: "#2E7D32", accent: "#A5D6A7" },
  "اللغة الإنجليزية": { primary: "#1565C0", accent: "#90CAF9" },
  "العلوم": { primary: "#6A1B9A", accent: "#CE93D8" },
  "الرياضيات": { primary: "#EF6C00", accent: "#FFCC80" },
  // افتراضي: يبقى النافي الحالي كما هو
};
```
يُطبَّق كمتغيرات CSS على `AppShell` عند دخول المدرس، حسب `Subject.theme_key`.

### ز) إطلاق المهام والواجبات
AI يقترح نشاطاً/واجباً بناءً على `extracted_text` للدرس (نفس استدعاء AI في الخطوة د، أو
استدعاء تكميلي خفيف). يُنشئ اختيارياً `ElectronicHomework` (10-15 سؤال). **قرار نطاق مطلوب**
حول واجهة حل الطالب — قسم 15.

### ح) لوحة الدرجات اللحظية
كل حدث (`SessionEvent`) يُدفع للوحة فوراً. الأحداث المطلوب تسجيلها: تقييم واجب، إجابة سؤال،
تقييم نشاط، حصول على واجب، حصول على تقييم. **الفرق عن التصميم الحالي:** كل حدث الآن سجل
دائم قابل للاسترجاع، وليس `useState` مؤقت.

### ط) ملخص نهاية الحصة → يُحفظ في `SessionRecord` كاملاً
الحقول: حاضرون، غائبون، عدد الأسئلة، عدد المشاركين، حالة إطلاق المهام، مدة الحصة، مدة الشرح،
التمديد إن وجد، ملاحظات عامة، أهم الأحداث (يُشتق تلقائياً من `SessionEvent` البارزة، مثل
غياب متأخر).

---

## 8) التقييمات والغياب (`teacher.assessments.tsx`) — قسم جديد بالكامل

أربع أدوات فرعية (تبويبات داخل نفس الصفحة، وليست صفحات منفصلة، حفاظاً على البساطة):

1. **الحضور والغياب**: قائمة طلاب بزر حاضر✓/غائب✕ لكل حصة سابقة — تعديل بأثر رجعي
2. **تقييم الواجب المنزلي**: قائمة طلاب + إدخال Score، مرتبط بـ`AssessmentScore
   (category:"homework", source:"manual")`
3. **تقييم المهام والأنشطة**: نفس النمط، `category:"activity"`
4. **السلوك**: تقييم سلوك حر (مقياس افتراضي 3 مستويات: إيجابي/محايد/يحتاج انتباه — **قرار
   مطلوب تأكيده أو تحديد مقياس بديل، قسم القرارات رقم 5**)، `category:"behavior"`

**عرض عام للطلاب** أسفل الصفحة: لكل طالب (الاسم، الحضور، متوسط الدرجات، الواجبات، الأنشطة،
الأسئلة، السلوك، المستوى الحالي) + قسمان بارزان: **أعلى الطلاب أداءً** و**يحتاجون تحسين**،
كل واحد بسبب مختصر (نفس منطق التصنيف في القسم 6-د/هـ لكن معروض هنا بتفصيل أكبر لأن هذا
مكان الإدارة لا الداشبورد السريع).

---

## 9) الخطة والمنهج (`teacher.curriculum.tsx`) — قسم جديد، "ذاكرة التشغيل"

### أ) البنية
عرض شجري: `CurriculumUnit → CurriculumLesson[]` — لكل وحدة: عدد الدروس، المدة المخططة،
نسبة الإنجاز. لكل درس: الحالة (تم / قيد التنفيذ / لم يبدأ)، وإن كان مرتبطاً بـ`Lesson`
فعلي (PDF مرفوع + مُدرَّس)، يظهر رابط مباشر له.

### ب) آلية "ذاكرة التشغيل" (Auto-linking) — التنفيذ الفعلي

هذا القسم **لا يحتوي منطقاً مستقلاً**؛ هو **عرض** يقرأ من الكيانات الأخرى بعد كل حدث:

| الحدث في وضع الحصة | الأثر التلقائي في الخطة والمنهج |
|---|---|
| رفع PDF جديد | `Lesson` جديد يظهر مرتبطاً بـ`CurriculumLesson` (المدرس يختار أي درس مخطط يقابله عند الرفع) |
| بدء/إنهاء التدريس | `CurriculumLesson.status` يتحدث تلقائياً (`in_progress` → `done`) |
| توليد أسئلة | تظهر كـ"أنشطة مرتبطة" أسفل الدرس في العرض |
| إطلاق واجب | يظهر مرتبطاً بالدرس |
| حل الطلاب للواجب | (خارج نطاق اليوم — قسم 15) تظهر النتائج هنا لاحقاً |
| إكمال كل دروس الوحدة | `نسبة الإنجاز` تُعاد حسابها تلقائياً (`count(done) / count(total)`) |

لا حاجة لـ"مزامنة" يدوية — لأن كل البيانات مصدرها واحد (`data-store.ts`)، والصفحة فقط
تستعلم وتعرض. هذا امتداد طبيعي لمبدأ Phase 0 (مصدر مركزي واحد).

---

## 10) نظام الذكاء الاصطناعي — التصميم الهندسي الكامل

**الإجابة على "هل تدريب Agent هو الأفضل؟" — لا.** الحل الأصح هندسياً: **دالة خادم واحدة ثابتة
(Server Function)** بمدخل PDF/نص ومخرج JSON محدد الشكل، بـ System Prompt **مكتوب مرة واحدة
في الكود** (ثابت، مُراجَع، له نسخة/version)، تُستدعى برمجياً — **وليس محادثة تُكرَّر تعليماتها
يدوياً في كل مرة**. الفرق: Agent محادثة = عرضة لتفاوت النتائج والتكلفة العشوائية. Function
بمخرج JSON مُقيَّد (Structured Output) = نتيجة متوقعة الشكل دائماً، قابلة للتحقق برمجياً قبل
الحفظ.

| السؤال | الإجابة الهندسية |
|---|---|
| ما نموذج/خدمة AI المناسبة؟ | نموذج قادر على قراءة نصوص طويلة ومخرجات JSON منظّمة (Claude API عبر Anthropic مناسب تماماً لهذا الاستخدام) |
| هل نحتاج API؟ | نعم، إلزامي — **قرار مطلوب: هل مفتاح API جاهز الآن أم نبني Stub فاضياً حالياً (القرار المسبق المتفق عليه)** |
| أين تتم المعالجة؟ | Server Function واحدة (استخراج النص من `PDF` client-side أو server-side ثم استدعاء الـAPI من السيرفر فقط — أبداً من المتصفح مباشرة، حماية للمفتاح) |
| كيف يُخزَّن الـPDF؟ | لا يُخزَّن دائماً بالبنية الحالية (لا backend حقيقي) — يُعالَج لحظياً، يُحتفَظ باسم الملف فقط للعرض. التخزين الدائم الحقيقي = Supabase Storage لاحقاً |
| كيف تُخزَّن الشرائح الناتجة؟ | `LessonSlide[]` و`QuizQuestion[]` داخل `Lesson` في `data-store.ts` — نصوص خفيفة، لا مشكلة تخزين |
| هل نحفظ النص المستخرج؟ | نعم إلزامي (`Lesson.extracted_text`) — يمنّع إعادة الاستخراج + يتيح "إعادة التوليد" لاحقاً بدون رفع الملف من جديد |
| كيف نتعامل مع التكلفة؟ | التخزين بـ`content_hash` يمنع إعادة معالجة نفس الملف نهائياً — أكبر موفّر تكلفة عملي |
| كيف نمنع تكرار المعالجة؟ | `content_hash` (SHA-256) — مذكور في القسم 7-د خطوة 3 |
| كيف نحتفظ بالنسخة الأصلية؟ | غير ممكن دائماً بالبنية الحالية (قيد حقيقي، ليس تجاهلاً) — قابل للحل عند إضافة Supabase Storage |
| كيف نعيد توليد العرض؟ | زر "إعادة التوليد" يستخدم `extracted_text` المخزَّن فعلاً (لا حاجة لرفع الملف تاني) ويستدعي نفس الـFunction بنسخة جديدة |
| كيف نتعامل مع فشل الـAI؟ | `Lesson.ai_status = "failed"` + `ai_error` نص واضح + زر "إعادة المحاولة" — الحصة لا تتعطل، المدرس يقدر يكمل يدوياً لو الوقت ضاغط |
| كيف نضمن عدم ضياع البيانات؟ | كل خطوة تُكتب في `data-store.ts` فور نجاحها (استخراج النص يُحفظ منفصلاً عن توليد الشرائح) — لو فشلت خطوة متأخرة، النص المستخرج لا يضيع |

---

## 11) الأمان وعزل البيانات

كما في القسم 4-د: العزل عبر دوال `data-store.ts` المُقيَّدة بـ`teacher_id`، وليس إخفاءً بصرياً.
**قيد صريح يجب الإفصاح عنه:** بما إن النظام كله حالياً Frontend بدون backend حقيقي، **لا
يوجد Authorization حقيقي غير قابل للتجاوز 100%** (مستخدم متمرس بالمتصفح يقدر نظرياً يتلاعب
بالـlocalStorage). هذا مقبول تماماً في مرحلة العرض التجريبي الحالية، لكنه **ليس أماناً
إنتاجياً حقيقياً** حتى يُضاف backend فعلي بـRLS على مستوى قاعدة البيانات (Supabase). يُذكر
هذا صراحة حتى لا يُفهم أن "العزل المنطقي" = "أمان إنتاجي جاهز للعملاء".

---

## 12) Frontend Components — الملفات بالتحديد

### تُعدَّل:
`src/routes/teacher.index.tsx`, `src/routes/teacher.session.tsx`, `src/config/roles.ts`,
`src/types/index.ts`, `src/lib/data-store.ts`, `src/components/session/SessionSteps.tsx`

### تُنشأ:
```
src/routes/teacher.assessments.tsx
src/routes/teacher.curriculum.tsx
src/components/teacher/StudentClassificationCard.tsx
src/components/teacher/TeacherNoteInput.tsx
src/components/session/PdfUploadBox.tsx
src/components/session/InteractiveSlideViewer.tsx
src/components/session/TimerExtendDialog.tsx
src/components/session/FairRandomPicker.tsx      // منطق الخوارزمية، لا واجهة فقط
src/components/curriculum/UnitTree.tsx
src/components/curriculum/LessonLinkCard.tsx
src/lib/ai/lesson-pipeline.ts                    // الـServer Function + الـSystem Prompt الثابت
src/lib/subject-themes.ts
src/hooks/use-content-hash.ts
```

---

## 13) State Management
امتداد مباشر لنمط Phase 0: كل الكيانات الجديدة (`Lesson, SessionRecord, SessionEvent,
AssessmentScore, RandomPickLog, TimerExtension, CurriculumUnit, CurriculumLesson,
ElectronicHomework, Subject, Grade`) تُضاف بنفس نمط `localStorage + subscribe/emit`
الموجود في `auth.ts` و`data-store.ts` — **لا نمط جديد، لا مكتبة state إضافية**.

---

## 14) استراتيجية التخزين (PDF Files)

**محدود بالبنية الحالية** — موضّح بالتفصيل في القسم 10. الحل المؤقت: معالجة لحظية + الاحتفاظ
بالنص المستخرج فقط. **حين يُضاف backend حقيقي (Supabase)**: `Storage bucket` لكل مركز
(`center_id`)، الملف يُرفَع فعلياً، `Lesson.source_file_url` يشير له، والمعالجة تبقى بنفس
الـPipeline لكن تُستدعى من Storage بدل الرفع المباشر.

---

## 15) قرارات مطلوبة من صاحب المشروع

هذه نقاط لم تُذكر بوضوح كافٍ في الرسالة الأصلية أو فيها تعارض ظاهري يحتاج قراراً منك تحديداً
(وليس قراراً هندسياً بحتاً يمكن اتخاذه بالنيابة عنك):

| # | القرار | لماذا مهم |
|---|---|---|
| 1 | **مفتاح AI API** — جاهز الآن للتفعيل الفعلي، أم نبني الـPipeline كاملاً بـStub فاضٍ (يُظهر نتيجة تجريبية ثابتة) لحد ما الاشتراكات تتظبط؟ | يحدد هل Phase 2 قابل للاختبار الحقيقي اليوم أم شكلياً فقط |
| 2 | ~~مراجعة المدرس للمحتوى المولَّد قبل استخدامه~~ — **✅ محسوم نهائياً**: **لا توجد شاشة مراجعة إلزامية قبل الاستخدام.** المحتوى (شرائح + أسئلة) يظهر ويُستخدم مباشرة فور التوليد بدون أي بوابة اعتماد. **لكن** يجب أن يبقى **قابلاً للتعديل من المدرس في أي وقت لاحق** عبر زر "تعديل" متاح دائماً على كل شريحة/سؤال. ⚠️ **تصحيح:** هذا الزر **لم يُبنَ بعد فعلياً** في Phase 2 (كان افتراضاً خاطئاً من كاتب هذا المستند) — راجع القسم 18 لحالته الحالية. | — |
| 3 | **حدود التصنيف الثلاثي** (ممتاز/متوسط/يحتاج متابعة) — القيم الافتراضية المقترحة في القسم 6-د (`85%`, `60%`, `75%` حضور) مناسبة، أم عندك معيار أكاديمي مختلف؟ | يؤثر على كل صفحات التصنيف (Dashboard + Assessments) |
| 4 | **قاعدة Score الالتزام بالتايمر مع التمديد** (القسم 7-ج) — النسبة المقترحة منطقية، أم تفضّل نظام نقاط مختلف (مثلاً خصم ثابت لكل دقيقة تمديد)؟ | يؤثر على مؤشر "الالتزام بالتايمر" في KPIs |
| 5 | **مقياس تقييم السلوك** — اقترحنا 3 مستويات افتراضية (إيجابي/محايد/يحتاج انتباه)، أم عندك مقياس مدرسي/تربوي محدد تريد استخدامه؟ | يحدد شكل واجهة "السلوك" في قسم التقييمات |
| 6 | **نطاق الواجب الإلكتروني** — نبني اليوم إنشاء المدرس للواجب فقط (`ElectronicHomework` من جهة المدرس)، ونؤجل واجهة "حل الطالب له" لدور الطالب لاحقاً (خارج نطاق اليوم أصلاً)، صح؟ | يمنع فتح ملف بوابة الطالب وسط شغل المدرس اليوم |
| 7 | **ربط حساب تسجيل الدخول بمدرس محدد** (`Teacher.user_id`) — هل نبنيه اليوم كجزء من Phase 1 (ضروري عملياً لتفعيل "أنا مين" الحقيقي)، أم مؤقتاً نستخدم `teachers[0]` كمستخدم افتراضي مثل الوضع الحالي؟ | يحدد صعوبة/نطاق Phase 1 |

---

## 16) اقتراحات إضافية (مقيَّدة بعدم زيادة التعقيد)

- **زر "إعادة توليد الأسئلة فقط"** (بدون إعادة الشرائح) — مفيد عملياً لأن المدرس أحياناً
  عايز أسئلة تانية لنفس الدرس بدون إعادة كل الـPipeline
- **مؤشر "آخر مرة اتسحب فيها كل طالب"** ظاهر بصرياً في لوحة الدرجات اللحظية أثناء مرحلة
  الأسئلة — يبني ثقة المدرس في عدالة التوزيع الآلي (شفافية الخوارزمية)
- **علم صغير على الدرس في الخطة والمنهج** لو `ai_status = "failed"` — يذكّر المدرس يرجع
  يعيد المحاولة قبل الحصة القادمة بدل ما يفاجأ وقت التدريس

---

## 19) ملحق — إعادة تنظيم لوحة المدرس الرئيسية (بعد تصليح الأخطاء الثلاثة)

> **لا يُنفَّذ إلا بعد تأكيد المشكلات 1، 2، 3 (راجع تسلسل الرسائل) وتأكيد صاحب المشروع الصريح
> بالبدء.** هذا ملحق شكل وعرض وربط بيانات، صفر تعديل على منطق `data-store.ts` الأساسي إلا
> حيث يُذكر صراحة.

### أ) موجود بالفعل — لا يُبنى من جديد، فقط يُربط/يُراجَع
- جدول المجموعات: الشكل جاهز، بانتظار بيانات حقيقية فقط
- أرقام الكروت: قيد التصليح في المشكلات الحالية
- محرك التفسير (`getPerformanceLabel` + `diagnoseWeakPoint` من §6): موجود ومبني، يحتاج **ربطه**
  بالأماكن الجديدة أدناه، وليس إعادة بناء
- إضافة طالب من الإدارة مع ربط تلقائي بمجموعات مدرّسيه حسب صفه ومادته (§7): موجود جزئياً —
  يُراجَع للتأكد من مطابقته 100% لما يلي، لا يُبنى من الصفر

### ب) تعديلات شكل وعرض
- اسم المدرس بخط أكبر وضوحاً، مع "مرحباً أستاذ/ة [الاسم]" بدل الشكل الحالي
- أيقونات مادة حقيقية معبّرة (ليست أيقونات عامة) لكل مادة من الخمسة
- إعادة ضبط أحجام الخطوط: العناوين أعلى الصفحة والنصوص تحت الكروت — تكبير معتدل للوضوح، وليس
  تغييراً جذرياً
- ⚠️ **نقطة غير مؤكدة من صاحب المشروع** — أشار لعنصر "وضع الحصة" أعلى يسار الصفحة طُلب تعديله
  في نقاش سابق ولم يُنفَّذ، لكن التفاصيل الدقيقة غير واضحة حتى الآن. **لا تُنفَّذ هذه النقطة
  تحديداً قبل توضيح إضافي.**

### ج) مكوّنات جديدة كلياً
1. **شارت "أعلى 5 طلاب" لكل مجموعة على حدة** (وليس مجمّعين) — قسم منفصل لكل مجموعة من مجموعات
   المدرس
2. **شارت مقارنة أداء بين المجموعات** — يوضح أي مجموعة (من مجموعات المدرس نفسه) أعلى مستوى
   بين أقرانها في نفس المرحلة الدراسية
3. **"يحتاجون متابعة" مُعاد تصميمه**: يجمع من كل مجموعات المدرس معاً، لكل طالب سطر مستقل
   يشرح **لماذا** هو في هذا التصنيف — يُستخدم `diagnoseWeakPoint` الموجود بالفعل من §6، هذا
   عرض جديد لنتيجة دالة موجودة
4. **أيقونة "المجموعات" جانبية جديدة** — عند الضغط، تعرض بطاقة لكل مجموعة (بعددها الفعلي:
   3، 4، 5... حسب عدد مجموعات هذا المدرس تحديداً)، وعند اختيار مجموعة تظهر كل طلابها ومستوياتهم
5. **"الخطة والمنهج" مُعاد تنظيمها حسب المجموعة أيضاً** — ليس فقط حسب المادة العامة؛ المنهج
   والخطة الزمنية المرفوعة (فهرس + مخطط زمني لكل مرحلة) يُربَطان بمجموعة/مرحلة محدَّدة

### د) قاعدة عامة صريحة من صاحب المشروع (تنطبق على كل المشروع، ليس هذا الملحق فقط)
**"أي رقم في أي كارت في أي مكان في المشروع، لازم يكون رقماً حقيقياً مُشتقاً من بيانات فعلية —
صفر أرقام ثابتة/تجريبية مكتوبة يدوياً."** هذا مبدأ عام يُراجَع به أي عمل مستقبلي، وليس خاصاً
بلوحة المدرس وحدها.

---

## 18) نقاط متابعة معلّقة — **قرار نهائي: تُقفَل الثلاثة الآن، قبل بدء Phase 5**

| النقطة | القرار | التفاصيل المطلوبة |
|---|---|---|
| `Teacher.timer_compliance` غير محسوب فعلياً | ✅ يُنفَّذ الآن | يُحسب من `SessionRecord`/`TimerExtension` الفعليين (متوسط `duration_seconds ÷ planned_duration` آخر 30 يوم لكل مدرس)، يستبدل الرقم الثابت في لوحة المدرس ولوحة المالك (`owner.compliance.tsx`) |
| زر "تعديل" على الشرائح/الأسئلة المولَّدة غير موجود | ✅ يُنفَّذ الآن | يُضاف زر تعديل بسيط متاح دائماً على كل `LessonSlide` وكل `QuizQuestion` داخل عارض وضع الحصة — تعديل نصي مباشر (inline)، يُحفظ فوراً في `data-store.ts`، بدون خطوة "اعتماد" منفصلة (يطابق قرار #2 المحسوم) |
| الحضور "آخر حالة" بس، مش سجل تاريخي | ✅ **قرار صاحب المشروع: مطلوب سجل تاريخي كامل لكل حصة سابقة** | يتطلب إضافة `session_id` (يربط بـ`SessionRecord`) إلى `AttendanceRecord` إن لم يكن موجوداً بالفعل. تبويب "الحضور والغياب" في `teacher.assessments.tsx` يعرض شبكة: كل طالب × كل حصة سابقة لمجموعته (تاريخ + حالة)، قابلة للتعديل بأثر رجعي لكل خلية على حدة، وليس "آخر حالة" فقط |

---

## 17) خطة التنفيذ على مراحل

| Phase | المحتوى | معيار النجاح (Testable) |
|---|---|---|
| **1** | Architecture + DB Schema (القسم 4) + إعادة هيكلة التنقل (القسم 5) + لوحة المدرس كاملة (القسم 6) | فتح لوحة مدرسين مختلفين يعرض بيانات كل واحد منفصلة تماماً، أرقام KPIs حقيقية 100% |
| **2** | وضع الحصة: التقسيم الزمني الجديد + التمديد (7-ج) + رفع PDF والـAI Pipeline (7-د، القسم 10) + عارض الشرائح التفاعلي + Theme حسب المادة | رفع نفس ملف PDF مرتين لا يستدعي AI مرتين (يتحقق من الـhash)، فشل AI لا يعطّل الحصة |
| **3** | الأسئلة العشوائية بالتوزيع العادل (7-هـ) + إطلاق المهام (7-ز) + لوحة الدرجات اللحظية كسجل دائم (7-ح) + ملخص نهاية الحصة (7-ط) | سحب 10 أسئلة متتالية لا يكرر نفس 3 طلاب، `SessionRecord` يُحفَظ فعلياً ويظهر لاحقاً |
| **4** | التقييمات والغياب (القسم 8) كاملاً | تعديل درجة قديمة بأثر رجعي يعمل، فصل auto/manual واضح في البيانات |
| **5** | الخطة والمنهج (القسم 9) + آلية الربط التلقائي | رفع درس جديد من وضع الحصة يظهر فوراً في الخطة والمنهج بدون أي إجراء يدوي إضافي |

كل Phase: `bun run dev` يعمل بدون كسر — قبل الانتقال للتالي، تأكد إن owner/staff/student/
parent/visitor لم تتأثر بأي تعديل (خصوصاً owner.compliance وowner.students اللي بيقرأوا
من نفس بيانات المدرسين والمجموعات).