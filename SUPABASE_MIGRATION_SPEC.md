<!-- SUPABASE_MIGRATION_SPEC.md — الانتقال من localStorage إلى قاعدة بيانات حقيقية.
     ضعه في جذر المشروع جنب باقي المستندات. -->

# الربط الكامل بـ Supabase — من الألف للياء

## 0) السياق والقيد الزمني
مطلوب إنجاز هذا كاملاً قبل الخميس. القرار المعتمد: **فصل بيانات العملاء عبر `center_id` في
كل استعلام + اتصال من السيرفر فقط بمفتاح خدمة (Service Role)**، وليس RLS كامل مع Supabase
Auth حقيقي — هذا الأخير يُضاف كتقوية أمان لاحقة بعد الإطلاق مباشرة، ومسجَّل كبند دَين تقني
صريح، ليس مهملاً.

## 1) استخراج Schema من الكود الحالي (مصدر الحقيقة)
لا تخترع أسماء حقول جديدة. اشتق كل جدول SQL مباشرة من `src/types/index.ts` الحالي —
كل Interface هناك = جدول، كل حقل = عمود بنفس الاسم ونوع مطابق. حافظ على `center_id` في
كل جدول له علاقة ببيانات عميل (كل الجداول تقريباً، ما عدا جدول `centers` نفسه).

## 2) بناء الجداول (Supabase Migrations)
- أنشئ ملفات migration SQL رسمية (`supabase/migrations/`)
- Primary keys UUID، Foreign keys بعلاقات واضحة (`teacher_id → teachers.id` إلخ)
- Indexes على كل `center_id` وكل foreign key يُستعلم عنه كثيراً (`group_id`, `student_id`, `lesson_id`)
- جدول `accounts` منفصل لحسابات الدخول (identifier فريد زي `TCH-2001`/`STD-10234`، password،
  role، ومعرّف الكيان المرتبط) — يطابق منطق `auth.ts` الحالي، بدون التحول لـSupabase Auth الآن

## 3) طبقة الاتصال — من السيرفر فقط
- استخدم TanStack Start Server Functions كطبقة وسيطة بين الواجهة و Supabase
- مفتاح `SUPABASE_SERVICE_ROLE_KEY` يُستخدم **داخل السيرفر فقط**، لا يصل للمتصفح إطلاقاً
- كل Server Function تتحقق من `center_id` الخاص بالجلسة الحالية يدوياً قبل أي عملية قراءة/كتابة
- المتصفح لا يتصل بـSupabase مباشرة أبداً في هذه المرحلة

## 4) الهجرة والبذر (Migration + Seed)
- انقل كل بيانات `mock-data.ts` الحالية (المدرسين الخمسة، الطلاب التجريبيين، المجموعات،
  المنهج، الدرس التجريبي "أدوات تحديد المواقع") كـ**عميل تجريبي واحد كامل** (`center_id`
  واحد) في Supabase — هذا هو "المعمل" اللي هنكمل نلعب فيه
- تأكد من مطابقة كل قيمة seed للبيانات الحالية بالضبط (صفر فرق ملحوظ للمستخدم)

## 5) استبدال طبقة data-store.ts بالكامل
- كل دالة قراءة/كتابة في `data-store.ts` (اللي كانت بتقرا/تكتب `localStorage`) تتحول لاستدعاء
  Server Function بتتكلم مع Supabase
- **كل شاشات الإضافة/التعديل الموجودة فعلاً في التطبيق** (إضافة مدرس، إضافة طالب، رفع درس،
  تسجيل حضور، تقييم واجب... كل حاجة) تكتب لـSupabase مباشرة من خلال هذه الطبقة — بدون
  إعادة بناء أي واجهة، فقط تغيير الوجهة اللي بتتكتب فيها البيانات
- لا تحذف كود `localStorage` القديم فوراً — علّقه (Comment) أو اتركه خلف Feature Flag لحد
  ما نتأكد Supabase شغال 100%، سهولة التراجع لو ظهرت مشكلة

## 6) التحقق (نفس المنهجية المعتادة، زيادة خطوة واحدة)
- `tsc --noEmit`, `bun run build` نظيفين
- فحص كل الـroutes
- **إضافي:** تجربة فعلية للـCRUD كامل من جوه التطبيق نفسه (مش من لوحة Supabase): إضافة
  مدرس جديد، إضافة طالب، تعديل بياناته — والتأكد إنها فعلاً بتظهر في جداول Supabase

## 11) هوية العميل البصرية + رابط دخول مخصص + توسعة المراحل — 🆕 جديد

### أ) لون مميز لكل عميل (Tenant Accent — منفصل عن ألوان المواد)
- `centers` يكتسب عمود جديد `accent_color` (قيمة hex أو مفتاح مُعرَّف مسبقاً)
- في `/platform/new-center`: قائمة اختيار بصرية (6-8 ألوان عصرية جاهزة، ليست إدخالاً حراً)
  يختار منها صاحب المنصة وقت إنشاء العميل
- `AppShell.tsx` يقرأ `center.accent_color` ويطبّقه على **السايدبار فقط** (خلفية/تمييز) — نظام
  ألوان المواد الخمسة (`DESIGN_ATMOSPHERE_SPEC.md`) يبقى مستقلاً تماماً وبلا أي تغيير

### ب) رابط دخول مخصص لكل عميل
- `centers` يكتسب عمود `slug` (نص فريد صديق للروابط، يُشتق تلقائياً من اسم العميل عند
  الإنشاء، قابل للتعديل)
- مسار جديد `/login/$slug` — قبل أي مصادقة، يجيب اسم ولون هذا العميل تحديداً (بيانات غير
  حساسة، آمن عرضها قبل تسجيل الدخول) ويعرضهم في صفحة الدخول نفسها
- الرابط اللي يُسلَّم للعميل الحقيقي = `https://[الرابط الأساسي]/login/[الـslug بتاعه]`
- الرابط العام القديم (`/`) يبقى موجوداً كنقطة دخول عامة/لصاحب المنصة، لا يُحذف

### ج) توسعة المراحل من 6 إلى 9 (إضافة الإعدادي الآن، ليس لاحقاً)
- إضافة `gd-7`/`gd-8`/`gd-9` (أولى/تانية/تالتة إعدادي) لجدول `grades`، بنفس نمط الست
  الموجودين تماماً (الجدول أصلاً مصمَّم قابلاً للتمدد، هذا امتداد بيانات وليس تعديل بنية)
- `GradeSubject`: المراحل الإعدادية التلاتة تُمنح نفس الخمس مواد الممنوحة للرابعة-سادسة
  ابتدائي (عربي/إنجليزي/رياضيات/علوم/دراسات) ما لم يُحدَّد غير ذلك
- شاشة "إضافة طالب" تعرض كل التسع مراحل في قائمة الاختيار، لا الست فقط

---

## 7) بند دَين تقني مُسجَّل صراحة (بعد الإطلاق مباشرة، ليس اليوم)
الانتقال لـSupabase Auth حقيقي + تفعيل RLS كامل على مستوى قاعدة البيانات نفسها — يُضاف
كأولوية أولى بعد استقرار الإطلاق الأول، ليس جزءاً من هذه المهمة.

---

## 8) شاشة "إضافة عميل جديد" (Onboarding عميل) — 🆕 مطلوبة الآن

عند بيع النظام لعميل جديد، شاشة بسيطة (للمالك العام للنظام، ليس للعميل) تُدخل:
- اسم العميل/السنتر
- تفاصيله (رقم تليفون، عنوان)

عند الحفظ:
1. صف جديد في جدول `centers` بمعرّف (`center_id`) جديد
2. توليد يوزر نيم تلقائي بصيغة `الاسم_الأول.الاسم_الثاني + رقم عشوائي` (مثال:
   `ahmed.mahmoud47568`) + كلمة سر قوية عشوائية، مربوطين بحساب Admin لهذا العميل تحديداً
3. من هذه اللحظة، **أي بيانات (مدرسين/طلاب/موظفين) يضيفها هذا العميل تُحفظ تلقائياً
   بـ`center_id` بتاعه** — بدون أي إجراء إضافي من العميل أو من المالك
4. الرابط اللي بيتدّى للعميل **موحّد لكل العملاء** (نفس النظام المنشور)، والفرق بينهم بس
   بيانات الدخول

## 9) ⚠️ شرط قبول إلزامي قبل اعتبار الهجرة مكتملة

**لا تُعتبر هذه المهمة مكتملة إلا بعد اختبار العزل فعلياً، ليس افتراضاً:**
أنشئ عميلين تجريبيين مختلفين (`center_id` مختلف لكل واحد)، أضف طالباً باسم مميز لكل واحد
منهم، وتأكد بشكل قاطع (استعلام مباشر أو تجربة فعلية بحسابين مختلفين) إن حساب العميل الأول
**لا يقدر يشوف ولا حرف واحد من بيانات العميل الثاني**، والعكس. قدّم دليل هذا الاختبار
تحديداً ضمن التقرير النهائي — ليس افتراضاً نظرياً بناءً على وجود `center_id` في الجداول.

---

## 10) النسخ الاحتياطي — نوعان مختلفان

### أ) تصدير ذاتي للعميل (Self-Service Export)
زر "تصدير نسخة احتياطية" في لوحة المالك (`owner.*`) — عند الضغط، يُولَّد ملف Excel (استخدم
مكتبة SheetJS/xlsx الموجودة أصلاً ضمن الاعتماديات) يحتوي كل بيانات هذا العميل تحديداً
(`center_id` بتاعه بس): الطلاب، المدرسين، المجموعات، الدرجات، الحضور — كل شيت لكيان.
تصدير مباشر من جانب العميل نفسه، بدون تدخل من صاحب المشروع.

### ب) نسخ احتياطي على مستوى المنصة
- **Supabase Pro يوفر نسخاً احتياطية يومية تلقائية مدمجة بالفعل** — لا حاجة لبناء آلية
  موازية لهذا الجزء، فقط التأكد من تفعيلها في إعدادات المشروع على Supabase
- **إضافة مطلوبة فوق ذلك:** أداة إدارية (تُستخدم من صاحب النظام، ليست ظاهرة للعملاء) تصدّر
  بيانات `center_id` واحد بعينه بشكل منظم عند الطلب — لاسترجاع سريع لعميل واحد متأثر بمشكلة،
  بدون الحاجة لاسترجاع قاعدة البيانات كاملة