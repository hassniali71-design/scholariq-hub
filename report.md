# تقرير تقني شامل — ScholarIQ Hub

تقرير مبني على قراءة فعلية لكل ملفات المشروع (30 route، أكثر من 49 جدول قاعدة بيانات، ~7000 سطر في طبقة البيانات وحدها)، وليس على أسماء الملفات أو الوصف الموجود في `CLAUDE.md`. أول نتيجة مهمة، وأهم سبب لكتابة هذا التقرير أصلاً: **`CLAUDE.md` الحالي لا يعكس حالة المشروع الفعلية إطلاقاً** — انظر القسم 1.

---

## 0) ملخص تنفيذي

| # | الحقيقة |
|---|---|
| 1 | المشروع **متصل فعلياً بـ Supabase حقيقي** (لا localStorage-mock كما يصف CLAUDE.md)، وفيه **مركزان حقيقيان بالفعل** (`ctr-0001`, `ctr-1787005507750`) ببيانات تشغيلية حقيقية. هذا ليس ديمو بحت. |
| 2 | **لا يوجد Row Level Security إطلاقاً** على أي جدول، وهذا قرار موثّق صراحة كـ"دين تقني" في `SUPABASE_MIGRATION_SPEC.md` — لكن العزل الوحيد المطبَّق فعلياً هو فلترة `center_id` يدوياً، **بدون أي تحقق من الدور (role)**. |
| 3 | **أخطر ثغرة**: أي حساب مسجّل دخول (حتى طالب) يقدر يستدعي دوال الخادم العامة (`insertRow`/`updateRow`/`deleteRows`) مباشرة (مش من واجهة المستخدم) ويعدّل أو **يمسح جدول كامل** في مركزه — بما فيه رواتب، فواتير، درجات طلاب آخرين. |
| 4 | **كلمات السر مخزّنة نص صريح (plain text)** في قاعدة البيانات، تُقارَن بـ`===`، وتُعرض نص صريح في واجهة المالك (`owner.access.tsx`). حساب المالك التجريبي (بريد + باسورد) معروض بالكامل على صفحة الدخول العامة لأي زائر. |
| 5 | **تسريب إجابات الاختبارات**: بنك الأسئلة كاملاً (شامل `correct_index`) يتخزن كـ JSON داخل حقل نصي يُعرض حرفياً في بوابة الطالب — أي طالب يقدر يشوف إجابات الواجب الإلكتروني في الصفحة نفسها. |
| 6 | **ثغرة تسريب بيانات بين العائلات**: لو كود جلسة طالب/ولي أمر غير صالح، الكود يرجّع بيانات **طالب عشوائي آخر** بدل رفض الدخول (`resolveCurrentStudent`) — نفس فئة الخطأ اتصلحت للمدرسين لكن نُسيت هنا. |
| 7 | سكريبتات `scripts/reset-clean-slate.ts` و`reset-and-seed-real.ts` **تمسح بيانات حقيقية** لمركزين فعليين، بدون أي تأكيد (confirmation) أو تجربة جافة (dry-run)، وتتصل مباشرة بـ Service Role Key. |
| 8 | خلاف ذلك: جودة الهندسة عالية نسبياً — تعليقات دقيقة جداً، تصميم قاعدة بيانات متسق، أرقام الـ"764"/"22 مجموعة" المذكورة في CLAUDE.md كمشكلة **تم إصلاحها بالفعل**، ولا يوجد أي `dangerouslySetInnerHTML` خطير أو حقن SQL. |

---

## 1) تصحيح جوهري: CLAUDE.md مقابل الكود الفعلي

`CLAUDE.md` (المسجَّل في جذر المشروع) يصف مرحلة "ما قبل Phase 0": بيانات وهمية بالكامل في `mock-data.ts`، بدون أي اتصال حقيقي بقاعدة بيانات، ومطلوب بناء `data-store.ts` بنمط localStorage+subscribe/emit من الصفر.

**هذا الوصف قديم جداً ولم يعد صحيحاً.** الدليل من الكود نفسه:

- `src/lib/auth.ts:24` → `export const USE_SUPABASE = true;` مع دوال خادم حقيقية (`auth-functions.server.ts`) تتكلم مع Supabase.
- `src/lib/data-store.ts` (4086 سطر) فيه `USE_SUPABASE = true` أيضاً (`data-store.ts:110`)، وكل دالة Mutation (حوالي 90 دالة) بتعمل "تحديث محلي فوري ثم مزامنة مع الخادم" — نمط Optimistic UI حقيقي فوق Supabase، مش localStorage بحت.
- يوجد **24 ملف migration** في `supabase/migrations/` + **14 ملف إضافي** في `db/` (مسارات لا تعمل تلقائياً بسبب قفل منصة الاستضافة لمجلد migrations)، تُنشئ **49 جدولاً حقيقياً**.
- يوجد مستندات معمارية كاملة غير مذكورة إطلاقاً في CLAUDE.md: `SUPABASE_MIGRATION_SPEC.md`، `CURRICULUM_ENGINE_SPEC.md`، `TEACHER_MODULE_SPEC.md`، `PLATFORM_CLIENT_MANAGEMENT_SPEC.md`، `DEPLOYMENT_SPEC.md`، `DESIGN_ATMOSPHERE_SPEC.md`.
- يوجد طبقة "منصة" كاملة (`/platform/*`) لإدارة عملاء متعددين (multi-tenant SaaS حقيقي) — مفهوم غير موجود إطلاقاً في وصف CLAUDE.md.
- المشكلة الوحيدة التي وصفها CLAUDE.md وما زالت قائمة فعلياً هي بعض البيانات الوهمية المتبقية في صفحات الطالب/ولي الأمر تحديداً (تفصيل في القسم 7) — لكن ليس بالحجم أو بالسبب الموصوف.

**الأثر العملي**: تنفيذ خطة "Phase 0" من CLAUDE.md حرفياً كان سيعني إعادة بناء طبقة بيانات موجودة ومكتملة بالفعل، وربما **الرجوع للخلف** (إلغاء الاتصال بـSupabase الحقيقي لصالح localStorage-mock). التوصية: **تحديث CLAUDE.md فوراً** ليعكس الوضع الحالي قبل أي عمل هندسي آخر عليه، حتى لا يُبنى أي قرار مستقبلي على وصف خاطئ.

---

## 2) الستاك التقني الفعلي

- **TanStack Start** (SSR كامل) + **React 19** + **TanStack Router** (file-based) + **TanStack Query** مُجهّز.
- **Supabase** (`@supabase/supabase-js@2.112.3`) — عبر عميل خادم واحد فقط بمفتاح Service Role (`getSupabaseAdmin`)، لا يوجد أي استخدام لـ anon key أو اتصال من المتصفح مباشرة.
- **Tailwind CSS v4** + **shadcn/ui** (style: new-york) + **recharts** + **xlsx** (تصدير Excel) + **Zod**.
- **Bun** كمدير حزم (`bun.lock`)، لكن أوامر التشغيل الفعلية في `package.json` تستخدم `vite` مباشرة (`vite dev`, `vite build`) — لا تعارض، فقط bun هو الـ runner.
- **Nitro** + إعداد Cloudflare Workers للنشر (`DEPLOYMENT_SPEC.md`, `scripts/fix-cloudflare-chunks.ts`).
- `tsconfig.json`: `strict: true` مع خيارات صارمة إضافية (`noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`) — لكن `noUnusedLocals`/`noUnusedParameters` مُعطَّلين، ونفس الشيء في ESLint (`@typescript-eslint/no-unused-vars: "off"`) — هذا يفسّر وجود متغيرات/imports ميتة معلَّقة بـ`void x;` بدل حذفها (انظر القسم 7).

---

## 3) حجم وبنية المشروع

- **30 ملف route** تحت `src/routes/` (owner: 8، staff: 5، teacher: 6، student: 2، parent: 2، platform: 4، login/index: 3).
- **11 مجلد components**: `owner`, `platform`, `staff`, `teacher`, `curriculum`, `session`, `tasks`, `dashboard`, `layout`, `auth`, `ui`.
- **طبقة بيانات ضخمة**: `data-store.ts` (4086 سطر)، `data-functions.server.ts` (680 سطر، طبقة CRUD عامة فوق 44 جدولاً)، بالإضافة إلى `auth-functions.server.ts`، `platform-functions.server.ts`، `owner-metrics.ts`.
- **49 جدول قاعدة بيانات** موزّعة على `supabase/migrations/` (30 جدولاً) و`db/` (9 جداول إضافية لا تُطبَّق تلقائياً — انظر القسم 4).
- **5 سكريبتات تشغيلية حساسة** في `scripts/` تتصل مباشرة بـService Role Key: بذر بيانات حقيقية، تصفير بيانات، إنشاء حساب منصة، backfill.
- لا يوجد أي ملف اختبارات (`*.test.ts`/`*.spec.ts`) في المشروع بالكامل — **صفر تغطية اختبارات آلية**.

---

## 4) قاعدة البيانات (Supabase)

### 4.1 الشكل العام
نموذج متعدد المستأجرين (multi-tenant): جدول `centers` هو الجذر، وكل جدول تقريباً يحمل `center_id`. جدول `accounts` موحّد لكل أدوار تسجيل الدخول الستة (owner/staff/teacher/student/parent/visitor) بمعرّف نصي فريد (`identifier`, مثل `TCH-2001`). بقية الجداول مقسّمة لعناقيد: تشغيلية (`students`, `teachers`, `groups`, `subjects`, `grades`)، محرك الحصة (`session_records`, `session_events`, `timer_extensions`, `assessment_scores`, `live_scores`)، مالية (`payments`, `booklets`, `booklet_sales`, `shift_closures`, `expenses`, `payroll_records`)، ومشتركة (`homework_tasks`, `quiz_results`, `whatsapp_logs`, `tasks`, `notifications`, `teacher_launches`).

### 4.2 مشكلة تشغيلية حقيقية: انقسام الـ migrations
`supabase/migrations/` يقفز من `0008` إلى `0012` مباشرة — الملفات `0009` و`0010` و`0011` موجودة **فقط** في مجلد `db/` (بسبب أن منصة الاستضافة تقفل مجلد `supabase/migrations` حسب تعليق في `db/0009_owner_control_tower.sql:1-2`). النتيجة: **تطبيق `supabase/migrations/` وحده على قاعدة بيانات جديدة سيفشل**، لأن `supabase/migrations/0013_notifications_extension.sql` يفترض وجود جدول `notifications` الذي يُنشأ فقط في `db/0009`. الكود يتحمّل هذا جزئياً عبر `withColumnFallback` (`data-functions.server.ts:355-371`) الذي يتجاهل الأعمدة غير الموجودة، لكن هذا تحايل وقت التشغيل وليس حلاً — **أي بيئة نشر جديدة (staging جديد، بيئة اختبار) ستحتاج تطبيق ملفات `db/` يدوياً بترتيب صحيح**، وهذا غير موثّق في `DEPLOYMENT_SPEC.md`.

### 4.3 غياب RLS و القيود
- **صفر سياسات RLS** في كل ملفات SQL (تم التأكد بالبحث الشامل عن `ROW LEVEL SECURITY`/`CREATE POLICY` — لا نتيجة). هذا قرار موثّق صراحة في `SUPABASE_MIGRATION_SPEC.md §0` و`§7` كدين تقني مؤجَّل لما بعد الإطلاق — لكنه أصبح أخطر الآن بعد وجود عملاء حقيقيين.
- عمود `accounts.password` من نوع `text` عادي (`0001_centers_and_accounts.sql:28`) — **لا تشفير إطلاقاً**، لا bcrypt ولا pgcrypto في أي مكان بالمشروع.
- جداول بدون Foreign Key رغم أنها تشير منطقياً لجداول أخرى: `schedule_slots.teacher_id` (نص حر بلا `references`)، `payroll_records.person_id`، `staff_permissions.account_identifier`، و`payments` بلا FK لـ`students` (مطابقة بالاسم/الكود نصياً فقط) — يسمح بسجلات يتيمة عند حذف/تعديل الحساب الأصلي بدون أي خطأ من قاعدة البيانات.
- `teacher_launches.file_data` (ملفات base64) بلا حد أقصى للحجم على مستوى قاعدة البيانات.
- `accounts.identifier` فريد **عالمياً عبر كل المراكز**، وليس فريداً لكل مركز — قرار مقصود لتسهيل البحث بمعرّف واحد، لكنه يعني عدم إمكانية تكرار نفس الكود بين عميلين مختلفين مستقبلاً.

---

## 5) نموذج الهوية والجلسات — أهم فجوة معمارية

الجلسة الحالية (`Session` في `auth.ts`) هي ببساطة كائن `{ role, full_name, identifier }` محفوظ في `localStorage` **بدون أي توقيع أو تشفير أو صلاحية انتهاء**. كل دالة خادم حسّاسة (`resolveCenterId` في `supabase-server.ts:64-75`) تستقبل هذا الـ`identifier` كباراميتر عادي من المتصفح وتبحث عن مركزه — **بدون أي تحقق أن المتصل فعلاً يملك هذه الهوية**.

هذا موثّق صراحة في تعليق الكود نفسه (`supabase-server.ts:58-63`): "المتصفح ممكن يزوّر الـ identifier بس لو خمّن identifier حقيقي لمركز تاني" — وهذا تحدٍ حقيقي فعلاً لأن:
- أكواد المدرسين: `TCH-` + 4 أرقام فقط (10,000 احتمال).
- أكواد الطلاب: `STD-` + 5 أرقام فقط (100,000 احتمال).
- لا يوجد Rate Limiting على `signIn` أو أي دالة خادم أخرى.

**الأخطر**: هذا لا يقتصر على القراءة. دوال الكتابة العامة نفسها (`insertRow`, `updateRow`, `upsertRow`, `deleteRows` في `data-functions.server.ts:373-448`) **لا تتحقق من نوع الحساب (role) إطلاقاً** — فقط من أن الـ`identifier` المُرسَل ينتمي لنفس `center_id`. بما أن `createServerFn` من TanStack Start تُنشئ endpoints حقيقية عبر HTTP، فإن:

- أي حساب طالب أو ولي أمر (حتى لو الواجهة الرسومية لا تعرض له أي زر) يقدر يستدعي هذه الدوال **مباشرة** ويعدّل درجات طالب آخر، رصيد مالي، رواتب مدرسين، أو صلاحيات الموظفين.
- `deleteRows` بدون تمرير `ids` (`data-functions.server.ts:434-448`) تمسح **الجدول بالكامل** لهذا المركز (`.delete().eq("center_id", centerId)` بدون `.in()`) — أي حساب في المركز يقدر يمسح كل المدفوعات أو كل الطلاب في ثانية واحدة.
- `createAccount`/`deleteAccount`/`updateAccountRow` (`auth-functions.server.ts`) نفس المشكلة: التعليق يقول صراحة "provisioning is owner/staff-only, enforced by the UI route guard" — أي أن الحماية الوحيدة هي إخفاء الزر في الواجهة، وليست تحققاً في الخادم.
- استثناء أخطر: `deletePlatformTeacherNote` (`data-functions.server.ts:516-566`) لا تستدعي `resolveCenterId` إطلاقاً — أي حساب من أي مركز يقدر يحذف ملاحظة منصة تخص مركزاً/مدرساً آخر تماماً.

**بالمقابل**، طبقة `/platform/*` (`platform-functions.server.ts`, دالة `assertPlatformCaller`) **مطبَّقة بشكل صحيح** — تتحقق فعلياً أن المتصل هو حساب `center_id = "platform"` قبل أي عملية. هذا يثبت أن الفريق يعرف النمط الصحيح ويطبّقه أحياناً، لكن لم يُعمَّم على الطبقة العامة (`data-functions.server.ts`) وطبقة الحسابات (`auth-functions.server.ts`) رغم أنهما أخطر بكثير لأنهما تغطيان 44 جدولاً.

كذلك، `signIn` (`auth-functions.server.ts:105`) يستخدم `.ilike("identifier", identifier)` بدون تهريب (escaping) لعلامات `%`/`_` — إدخال مثل `"%"` قد يطابق أي حساب بنفس الدور بدل تطابق دقيق (بينما كل الاستعلامات الأخرى في المشروع تستخدم `.eq()` دقيقاً بشكل صحيح — هذه حالة معزولة في نقطة الدخول الأخطر بالتحديد).

---

## 6) المشاكل الأمنية — مرتّبة حسب الخطورة

### 🔴 حرجة (Critical)

1. **لا تحقق من الدور في طبقة CRUD العامة** — أي حساب يقدر يقرأ/يكتب/يمسح أي جدول في مركزه، شامل الجداول المالية والصلاحيات. (`data-functions.server.ts:373-448`)
2. **`deleteRows` بدون `ids` تمسح الجدول كاملاً** لأي مركز، متاحة لأي حساب مسجَّل. (`data-functions.server.ts:434-448`)
3. **كلمات سر نص صريح** — تخزين ومقارنة بدون أي تشفير. (`0001_centers_and_accounts.sql:28`, `auth-functions.server.ts:109,382`)
4. **عرض كلمات السر نص صريح في واجهة المالك** — أي شخص يشوف شاشة owner.access.tsx يشوف كل كلمات سر الموظفين/المدرسين. (`owner.access.tsx:749,815`)
5. **حساب المالك التجريبي معروض بالكامل على صفحة الدخول العامة** مع زر تعبئة تلقائية — بيانات دخول حقيقية ظاهرة لأي زائر بدون تسجيل دخول. (`LoginCard.tsx:67-70,173-183`)
6. **تسريب بنك إجابات الاختبارات للطالب** — `correct_index` يتخزن ويُعرض حرفياً داخل بوابة الطالب لواجبات "online_homework". (`InteractiveActivityStudio.tsx:99-113`, معروض في `student.index.tsx:438-442`)
7. **تسريب بيانات عائلة أخرى عند جلسة غير صالحة**: `resolveCurrentStudent` (`data-store.ts:596-599`) يرجّع `state.students[0]!` بدل `null` — يعرض بيانات طالب عشوائي (درجات، رصيد مالي، سجل واتساب) بدون أي تحذير. نفس الخطأ **تم اكتشافه وإصلاحه فعلاً** في الجانب المقابل للمدرس (`use-current-teacher.ts:9-17` يوثّق ذلك) لكن نُسي هنا.
8. **جلسة غير موقّعة قابلة للتخمين** — `identifier` نص عادي بلا توقيع/انتهاء صلاحية، وأكواد المدرسين/الطلاب قصيرة رقمياً (4-5 أرقام) بدون Rate Limiting على تسجيل الدخول.
9. **سكريبتات تدمير بيانات حقيقية بدون حماية**: `scripts/reset-clean-slate.ts` و`reset-and-seed-real.ts` يمسحان بيانات مركزين حقيقيين فعلاً، بدون أي `--confirm` أو `--dry-run` أو نسخة احتياطية تلقائية قبل التنفيذ، بتشغيلة واحدة (`bun run scripts/...`).

### 🟠 عالية (High)

10. **`ILIKE` بدون تهريب في تسجيل الدخول** — يفتح احتمال مطابقة حساب غير مقصود بأنماط `%`/`_`. (`auth-functions.server.ts:105`)
11. **لا تحقق أن المدرس المتصل هو فعلاً صاحب المجموعة/الدرجة** — أي `teacher_id` يُرسَل من المتصفح يُقبل كما هو. (موثّق صراحة كدين مقبول في `data-store.ts:791-800`)
12. **`platform_teacher_notes` بلا حماية مركز** — الحذف لا يستدعي `resolveCenterId` إطلاقاً. (`data-functions.server.ts:516-566`)
13. **انقسام migrations** يجعل أي بيئة جديدة تفشل عند تطبيق `supabase/migrations/` وحدها (تفصيل في القسم 4.2).
14. **قصور معماري في "وضع الحصة"**: `sessionIdRef` يُعاد توليده بمعرّف جديد (`sess-${Date.now()}`) في كل مرة تُفتح الصفحة — إعادة تحميل المتصفح أثناء الحصة تكسر عدّاد المشاركين وربط "مين اتسأل بالفعل"، رغم أن الهدف المعلن هو "لا فقدان أبداً للتقدم". (`teacher.session.$groupId.tsx:212`)
15. **حالة تايمر الأنشطة/الامتحانات المباشرة تعيش في `useState` محلي** — التبديل بين تبويبات الشريط الجانبي أثناء نشاط/امتحان حي يوقف العدّاد بصمت بدون أي حفظ. (`ActivityRunner.tsx`, `ExamsCard.tsx`)
16. **`OwnerNotesCard`**: الملاحظات تُستهدف بدلو دور عام ("teacher"/"staff"/"all") بلا `teacher_id`/`center_id` — ملاحظة المالك لمدرس واحد تظهر لكل المدرسين. (`OwnerNotesCard.tsx:43-45`)

### 🟡 متوسطة (Medium)

17. مفاتيح خارجية مفقودة على أعمدة تبدو كـ FK نصياً (`schedule_slots.teacher_id`, `payroll_records.person_id`, `payments`↔`students`) — سجلات يتيمة صامتة.
18. `createAccount`/`deleteAccount`/`updateAccountRow` بلا تحقق دور خادمي (نفس فئة #1 لكن على مستوى الحسابات لا البيانات).
19. `fetchAllTablesForCenter` تجلب **كل صفوف 44 جدولاً** بدون Pagination عند كل تسجيل دخول/تحميل — ينمو بلا حد مع الوقت.
20. `syncBulkInsert` يرسل طلب HTTP منفصل لكل صف بدل Batch واحد (مثال: توزيع واجب على كل طلاب مجموعة). (`data-store.ts:142-151`)
21. توليد IDs بـ`Date.now()` داخل حلقة متزامنة (`startGroupSession`) — احتمال تصادم Millisecond بين طالبين، يفشل الحفظ بصمت.
22. رفع الملفات (اختبارات/مراجعات) يعتمد فقط على حد حجم لكل ملف (10MB) ونوع MIME **يرسله المتصفح نفسه** بدون فحص محتوى فعلي، وتُخزَّن كـ base64 داخل عمود نصي بلا حد إجمالي.

---

## 7) الأخطاء البرمجية (Bugs) — حسب البوابة

### المالك (Owner) / المنصة (Platform)
- `owner.students.tsx:517` — `addTeacherNote(..., teachers[0]?.id ?? selected.id, ...)`: لو مفيش مدرسين، الكود يستخدم **معرّف الطالب نفسه كمعرّف مدرس** — يفسد أي تجميع مستقبلي بحسب `teacher_id`.
- `owner.finance.tsx:90-91` — `marginBefore = grossBefore ? 100 : 0`: حساب هامش ربح وهمي، يرجع دايماً 100 تقريباً، لا يعكس أي نسبة حقيقية رغم عرضه كـ"هامش قبل الخصم".
- `owner.treasury.tsx:35` — متغير `settings` (إعدادات مالية) محسوب لكن غير مُستخدم إطلاقاً (كود ميت).
- `owner.access.tsx:804-806` — حذف الحساب من `data-store` وحذفه من `auth.ts` عمليتان منفصلتان بلا معاملة (transaction) — فشل إحداهما يترك الحساب في حالة نصف محذوفة.
- `owner.access.tsx:546-554` — مفتاح `localStorage` مخصَّص يدوياً (`staff_expected_salary`) خارج نمط `auth.ts`/`data-store.ts` المعتمد في المشروع، ولا يُقرأ في أي مكان آخر (كتابة ميتة، ومخالفة صريحة لقاعدة "لا تخترع نمط تاني" في CLAUDE.md نفسه).
- `platform.teacher-notes.tsx` — الصفحة الوحيدة بين صفحات `/platform/*` التي تستخدم `data-store.ts` (طبقة محلية) بدل دالة خادم حقيقية، رغم أنها الصفحة الوحيدة المفترض أنها **تعمل عبر كل المراكز** — يحتاج تأكيد أن الملاحظات فعلاً تنتشر بين مراكز مختلفة، لا محفوظة محلياً فقط.
- عدة imports/متغيرات ميتة مُعلَّقة بـ`void x;` بدل حذفها (`void ListTodo`, `void Calculator`, `void WEEKDAYS`) — بسبب تعطيل فحص "unused vars" في ESLint/tsconfig.
- تناقض في تأكيد العمليات الحساسة: بعض الحذوفات تمر بـ`OwnerPasswordConfirmModal` (تحقق خادمي حقيقي)، وأخرى بـ`window.confirm()` بسيط، وأخرى بلا أي تأكيد إطلاقاً — لا معيار واحد لأي عملية تُعتبر "خطيرة بما يكفي".

### الموظف (Staff)
- `staff.cashier.tsx:391-418` — حقل `reference` مُستخدَم لغرضين مختلفين (اسم محفظة إلكترونية / رقم مرجع الحوالة) بنفس متغير الحالة: اختيار محفظة يمسح الرقم المكتوب، وكتابة رقم تمسح اسم المحفظة المختارة — **لا يمكن حفظ الاثنين معاً أبداً**.
- `staff.shift.tsx:28` — `const [counted, setCounted] = useState(expected)`: القيمة الابتدائية تُلتقط لحظة التحميل فقط. لو اتسجلت دفعة جديدة أثناء فتح الشاشة، "المبلغ المتوقع" الحقيقي يتحدّث لكن القيمة المعروضة للموظف تفضل قديمة، فيحسب الفرق (`diff`) غلط بدون أن يلاحظ.
- `staff.tasks.tsx:47` — أثناء الـ SSR، `getSession()` يرجع `null` دايماً، فالصفحة المُصيَّرة من الخادم تعرض "صفر مهام" دايماً بغض النظر عن الموظف الفعلي، لحين اكتمال الـ hydration.
- لا تأكيد كلمة مرور أو مدير على `closeShift` رغم أنها عملية مالية حساسة (تقفيل وردية)، بعكس عمليات أخرى مشابهة في الخطورة.

### المدرس (Teacher) — "وضع الحصة"
- `teacher.session.$groupId.tsx:153-212` — التحقق من ملكية المدرس للمجموعة يحدث في `useEffect` وفي حارس render، لكن **كل الـ hooks فوقهم بالفعل قرأت بيانات المجموعة غير المصرَّح بها** قبل تنفيذ الحارس — لا تسريب فعلي حالياً (بيانات محلية)، لكن النمط خطير لو تحوّلت هذه القراءة لاستعلام شبكة مباشر مستقبلاً.
- `teacher.session.$groupId.tsx:231-235` — `attendanceStatus` يبحث بـ`student_id` فقط بلا فلترة بالمجموعة/التاريخ/الجلسة — قد يرجّع سجل حضور من يوم/مجموعة مختلفة تماماً لو ترتيب المصفوفة غير مضمون.
- `teacher.session.$groupId.tsx:259-301` — رفع الملف وإعادة محاولة توليد الدرس بـAI ملفوفة في `try/finally` **بلا `catch`** — أي خطأ يتحول لـ Unhandled Promise Rejection صامت، والمؤشر الدوّار يختفي بلا أي رسالة خطأ للمدرس.
- `use-countdown.ts:20-30` — استدعاء دالة ذات أثر جانبي (toast + تغيير حالة) **من داخل** دالة تحديث `setState` نفسها — نمط غير مدعوم رسمياً في React، وقد يتكرر مرتين تحت React Strict Mode.
- يوجد **3 تطبيقات منفصلة للعدّاد التنازلي** (`use-countdown.ts`, `SessionFreeTimer.tsx`, `ActivityRunner.tsx`) بدل توحيدها في هوك واحد — أي إصلاح مستقبلي لازم يتكرر 3 مرات.
- `BehaviorScoreColumn.tsx:67` — `value={busyId === s.id ? current : current}` — الشرطان متطابقان (كود ميت / خطأ نسخ-لصق).
- `AwardsAndAlertsPanel.tsx` (`MessageModal`) — لو المدرس مسح نص الرسالة بالكامل يدوياً، الكود يعيد ملء القالب الافتراضي تلقائياً في الـ render التالي، بمعنى **لا يقدر يرسل رسالة فارغة أو معدَّلة بالكامل** بسهولة.

### الطالب / ولي الأمر
- `student.index.tsx:41`, `parent.index.tsx` — استيراد مباشر لـ`studentAttendanceSeries` من `mock-data.ts` (سلسلة بيانات ثابتة) بدل اشتقاقها من `attendanceRecords` الحقيقية — **كل طالب يشوف نفس الشارت** بغض النظر عن حضوره الفعلي.
- `student.leaderboard.tsx:28-33` — الشارات (Badges) نص ثابت بالكامل، غير مشتقة من أي شرط حقيقي (نفس ما ذكره CLAUDE.md، ولا يزال قائماً).
- `student.leaderboard.tsx:38` — `leaderboard[0]!` — ينهار البرنامج (crash) لو لوحة الشرف فاضية.
- `parent.index.tsx:82-104` — لوحة "حالة اليوم" فيها 3 جمل نصية ثابتة بالكامل (وقت الحضور، مقارنة بالمتوسط، سرعة الإجابة) معروضة كأنها بيانات حية، رغم توفر بيانات `LiveScore` الحقيقية لعرضها فعلياً بدلها.

---

## 8) مشاكل الأداء

- **لا Pagination على مستوى قاعدة البيانات**: `fetchAllTablesForCenter` يجلب كل صف من 44 جدولاً في كل تسجيل دخول/تحديث صفحة — سينمو الحجم مع الوقت (حضور، مدفوعات، سجلات واتساب) بلا أي حد أو فلترة بالتاريخ.
- **N+1 عند الكتابة الجماعية**: توزيع واجب على مجموعة كاملة، أو إنشاء شرائح/أسئلة درس، يرسل طلب شبكة منفصل لكل صف بدل استدعاء Batch واحد.
- حسابات مكرّرة غير محفوظة (`useMemo`) داخل `.map()` في عدة صفحات (`owner.students.tsx` — `isTopPerformerInGroup` لكل طالب، `owner.finance.tsx` — مجموع الرسوم يُعاد حسابه مرتين)، غير مؤثرة على 6 طلاب تجريبيين، لكنها ستصبح ملحوظة مع نمو عدد الطلاب الحقيقيين.
- لا Pagination/Virtualization على أي قائمة طويلة محتملة (سجل المدفوعات، قائمة عملاء المنصة، سجل النشاط) — مقبول حالياً بحجم البيانات التجريبي، لكنه سيصبح مشكلة حقيقية مع نمو العملاء الحقيقيين.

---

## 9) ملاحظات جودة الكود والتوثيق

- **إيجابي**: التعليقات داخل الكود من أفضل ما رأيناه في مشروع من هذا الحجم — كل قرار معماري موثَّق بسبب صريح ومرجع لملف Spec، وأخطاء سابقة (مثل باغ `teachers[0]!`) موثّقة بتاريخها وسبب إصلاحها.
- **README.md قديم جداً** — لا يزال يصف تعليمات Lovable الأصلية (`npm i && npm run dev`)، ولا يذكر Bun، TanStack Start، أو Supabase إطلاقاً. أي مطوّر جديد يتبع الـ README سيفشل في التشغيل الصحيح.
- **CLAUDE.md قديم جداً** (تفصيل كامل في القسم 1) — يحتاج إعادة كتابة كاملة قبل استخدامه كمرجع لأي عمل قادم.
- `AGENTS.md` يحتوي فقط تحذيراً بخصوص مزامنة Lovable Git، لا تعليمات هندسية.
- تعطيل فحص المتغيرات غير المستخدمة (ESLint + tsconfig) يُخفي كوداً ميتاً بدل كشفه — يستحق إعادة النظر الآن بعد استقرار المشروع.
- **صفر اختبارات آلية** في مشروع بهذا الحجم والحساسية المالية — أي تعديل مستقبلي على `data-store.ts` (4086 سطر) لا يوجد ما يضمن عدم كسر منطق موجود.

---

## 10) التوصيات — مرتّبة بالأولوية

### فوري (قبل أي استخدام حقيقي إضافي)
1. تعطيل/تقييد سكريبتات `reset-clean-slate.ts` و`reset-and-seed-real.ts` (نقلها خارج المستودع أو إضافة تأكيد صريح + نسخة احتياطية تلقائية قبل التنفيذ).
2. إزالة عرض بيانات حساب المالك التجريبي من صفحة الدخول العامة المنشورة فعلياً (أو التأكد أنها بيئة تجريبية منفصلة تماماً عن المراكز الحقيقية).
3. إخفاء/تشفير عرض كلمات السر في `owner.access.tsx`.
4. إضافة تحقق من **الدور** (role) — وليس فقط `center_id` — داخل كل دوال `data-functions.server.ts` و`auth-functions.server.ts` الحسّاسة (خصوصاً `deleteRows` بلا `ids`، و`createAccount`/`deleteAccount`).
5. إصلاح `resolveCurrentStudent` ليرجّع `null` بدل `state.students[0]!` (نفس الإصلاح الذي تم للمدرس).
6. منع تسريب `correct_index` للطالب — فصل بنك الإجابات عن الحمولة (payload) المرسلة/المعروضة للطالب.

### قريب المدى
7. تفعيل تجزئة كلمات السر (bcrypt/argon2) وتصحيح جدول `accounts.password`.
8. توحيد ملفات migrations في مكان واحد قابل للتطبيق التلقائي، أو توثيق خطوات `db/*.sql` بوضوح في `DEPLOYMENT_SPEC.md`.
9. إضافة توقيع/انتهاء صلاحية حقيقي للجلسة بدل `identifier` الخام، أو على الأقل Rate Limiting على تسجيل الدخول.
10. تصحيح باغ الكاشير (`reference` field)، وباغ `staff.shift.tsx` (القيمة الافتراضية الثابتة)، وباغ `addTeacherNote` الاحتياطي.
11. تحديث `CLAUDE.md` و`README.md` بالكامل ليعكسا الوضع الحالي الحقيقي.

### متوسط المدى
12. إضافة Pagination لطبقة الجلب الكامل للبيانات (`fetchAllTablesForCenter`)، وتحويل الكتابة الجماعية لـ Batch حقيقي.
13. تفعيل RLS تدريجياً كطبقة دفاع ثانية فوق الفلترة اليدوية الحالية (كما هو موثَّق كخطة مؤجَّلة في `SUPABASE_MIGRATION_SPEC.md §7`).
14. توحيد منطق العدّاد التنازلي في هوك واحد مشترك بدل 3 تطبيقات منفصلة.
15. إضافة طبقة اختبارات آلية أساسية على الأقل لدوال `data-store.ts` المالية والحرجة.

---

*هذا التقرير مبني على قراءة كاملة للكود الفعلي بتاريخ إنشائه، وليس على توثيق أو أسماء ملفات فقط. كل نتيجة أعلاه مذكور معها المسار ورقم السطر التقريبي للتحقق المباشر.*
