<!-- PLATFORM_CLIENT_MANAGEMENT_SPEC.md — إدارة كاملة لكل العملاء من لوحة مدير المنصة. -->

# إدارة العملاء الكاملة — لوحة مدير المنصة

## 0) قاعدة تواصل إلزامية لهذه المهمة ولما بعدها

**كل تقرير يبدأ بملخص من 3-4 أسطر بالعربي البسيط، بدون أي مصطلحات تقنية**، يشرح: إيه اللي
اتعمل، هل نجح، وإيه المطلوب من صاحب المشروع (لو فيه حاجة). التفاصيل التقنية الكاملة تيجي
**بعد** الملخص، مش بدلاً منه. لو واجهت مشكلة حقيقية، اشرحها الأول بمثال أو تشبيه بسيط قبل
ما تدخل في تفاصيل الكود، عشان صاحب المشروع (مش مبرمج) يقدر يشارك في القرار.

---

## 1) سجل كل عميل — بيانات جديدة على `centers`

```ts
centers {
  ...existing fields,
  joined_at: TIMESTAMPTZ    // ⚠️ NEW — لحظة الإنشاء بالضبط (ساعة/دقيقة/يوم/شهر/سنة)، تلقائي 100%
  expires_at: TIMESTAMPTZ   // ⚠️ NEW — يُحسب تلقائياً = joined_at + سنة كاملة وقت الإنشاء
  status: "active" | "paused"  // ⚠️ NEW — افتراضي "active"
}
```

### بيانات العملاء الأربعة الموجودين حالياً (تصحيح رجعي — Backfill)
- لكل مركز موجود بالفعل: لو عنده `created_at` حقيقي في القاعدة، استخدمه كـ`joined_at`
- لو مفيش أي تاريخ حقيقي موجود (الحالة الأرجح لمركزين التجربة الأصليين)، استخدم **تاريخ
  ولحظة التنفيذ الفعلية** (يعني دلوقتي) كـ`joined_at` — ومنه يُحسب `expires_at` (سنة من دلوقتي)
- `status` لكل الأربعة = `active` افتراضياً

---

## 2) صفحة "كل العملاء" — جديدة بالكامل

مسار جديد (مثال: `/platform/clients`)، يعرض كل عميل في كارت/صف يحتوي:

| العنصر | التفاصيل |
|---|---|
| الاسم | اسم السنتر |
| الحالة | نشط / متوقف — مع **زر واحد** يبدّل الحالة فوراً |
| تاريخ الانضمام | منسّق (مثال: "12 أغسطس 2026 — 3:52 م") |
| تاريخ الانتهاء | نفس التنسيق |
| **مقياس تقدم بصري** | شريط أو دائرة تقدم توضح نسبة الوقت المتبقي من السنة (أخضر → أصفر عند اقتراب الانتهاء → أحمر لو انتهى فعلاً) |
| الرابط | قابل للنسخ بضغطة واحدة (`/login/$slug` الكامل) |
| البريد | بريد المالك اللي اتولّد وقت الإنشاء |
| زر تصدير | نفس أداة التصدير الموجودة بالفعل (§10-ب من `SUPABASE_MIGRATION_SPEC.md`)، لكن **متاحة هنا مباشرة لكل عميل من غير ما تدخل تكتب الـID يدوياً** |

### آلية الإيقاف/التشغيل
- عند "إيقاف" عميل: `status = "paused"` فقط — **البيانات لا تُحذف ولا تُمس إطلاقاً**
- محاولة دخول عميل موقوف تُرفض برسالة واضحة (مثال: "الاشتراك متوقف حالياً، تواصل مع الدعم")
  بدلاً من رسالة خطأ عامة
- "تشغيل تاني" يرجّع `status = "active"` فوراً — كل البيانات القديمة موجودة زي ما هي

---

## 3) إضافات مقترحة من عندي (مقيَّدة، مش تضخيم)

عشان "التحكم الكامل" اللي طلبته يبقى عملي فعلاً، وليس مجرد شكل:

1. **شريط ملخص أعلى الصفحة**: عدد العملاء الكلي، عدد النشطين، عدد اللي هينتهي اشتراكهم
   خلال 30 يوم — بيساعدك تعرف مين محتاج تتواصل معاه للتجديد من غير ما تفتح كل عميل لوحده
2. **زر "تمديد الاشتراك"** لكل عميل — يضيف مدة (شهر/سنة) على `expires_at` الحالي مباشرة،
   لحظة التجديد الفعلي، بدون الحاجة لحذف العميل وإعادة إنشائه
3. **بحث/فلترة** بالاسم أو بالحالة (نشط/متوقف) في نفس الصفحة
4. **"آخر نشاط"** لكل عميل (آخر مرة سجّل فيها أي حساب تابع له دخول فعلي) — يوضحلك مين بيستخدم
   النظام فعلاً ومين مسجّل بس وميستخدموش

---

## 4) شرط تحقق إلزامي
اعرض بيانات عميل حقيقي واحد فعلاً بعد التنفيذ (استعلام مباشر على القاعدة، وليس افتراضاً)،
تأكد إن التاريخ والمقياس البصري بيتغيّروا صح، وجرّب إيقاف عميل تجريبي والتأكد إن دخوله فعلاً
اترفض برسالة واضحة قبل ما ترجّعه نشط تاني.